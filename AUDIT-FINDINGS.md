# ‏AUDIT-FINDINGS — סריקת רספונסיביות + RTL (קריאה בלבד, לפני תיקונים)

- **עץ:** `fix/responsive-rtl-2026-08-12` @ בסיס `7844321` (‏origin/main)
- **שיטה:** ‏grep מכוון לפי `references/ai-failure-patterns.md` (‏responsive-craft) +
  רשימת ה-RTL של `hebrew-rtl-best-practices` + נתוני המטריצה המדודה (391 תאים) של #199.
- **הקשר:** האפליקציה RTL-first (`<html dir="rtl">` קבוע, אין מצב LTR) ואחרי מעבר
  רספונסיביות מדוד (#199). לכן חלק גדול מרשימות הסריקה כבר ירוק — מה שנרשם כאן הוא
  **מה שנשאר**, כולל ממצאים שהוכרעו "מכוון" עם הנימוק.

## סיכום ספירות — צ'קליסט הרספונסיביות

| בדיקה | ממצא | סטטוס |
|---|---|---|
| `100vh` חשוף | ‏3 מופעים — כולם זוגות `100vh`+`100dvh` (‏fallback מכוון) | ✅ תקין |
| ‏media queries מסוג `max-width` | ‏27 | ⚠️ דפוס מכוון — ראה F-10 |
| ילדי flex בלי `min-width:0` | ‏148 הגנות `min-w-0` קיימות; ‏0 גלישות נמדדו ב-391 תאים | ✅ |
| `overflow:hidden` על אב של sticky | ‏73 מופעי hidden; ‏0 כשלי sticky נמדדו (כותרות הגריד נבדקו חיות) | ✅ |
| ‏inputs מתחת ל-16px | ‏`field-input` = ‏17px תחת מגע (‏responsive.css:89); ‏INPUT-ZOOM=0 נמדד | ✅ |
| אסקלציית z-index | סולם מדורג 15→120, אין `9999` | ✅ |
| גלילה אופקית ב-320px | ‏0 מתוך 391 תאים (doc וגם main) — נמדד | ✅ |
| ‏safe-area / `viewport-fit` | קיים מ-#199 (‏layout.tsx + responsive.css) | ✅ |
| יעדי מגע 44×44 | ‏0 מתחת ל-44 · 0 חפיפות — נמדד `elementFromPoint` | ✅ |

## סיכום ספירות — צ'קליסט ה-RTL

| בדיקה | ממצא | סטטוס |
|---|---|---|
| `margin/padding/border-left/right`, `text-align: left/right`, `float` ב-CSS | **0** | ✅ הומר כולו ללוגי ב-#199 |
| מחלקות Tailwind פיזיות (`ml- mr- pl- pr- text-left …`) | **0** אמיתיות (‏`rounded-lg` הן false-positive של grep) | ✅ |
| `left:`/`right:` גולמיים ב-CSS | **5** — ראה F-2, F-6 | 🟡 2 לתיקון, 3 מכוונים |
| מחלקות `left-*`/`right-*` פיזיות ב-TSX | **4** — ראה F-1, F-5, F-7 | 🟡 2 לתיקון, 2 מכוונים |
| `row-reverse` | ‏40 מופעים — **דפוס מכוון ומתועד** (‏§7: primary צמוד לקצה השמאלי הפיזי) + מסך קפוא | ✅ מוכרע |
| `dir` על `<input>`/`<textarea>` | ‏197 שדות; ‏16 `dir="ltr"` טכניים; **0 `dir="auto"`** | 🔴 F-3 |
| צללים/גרדיאנטים כיווניים | צללים: כולם אנכיים/סימטריים · גרדיאנטים: ‏4 דקורטיביים (‏45° פסים) | ✅ אין כיוון משמעותי |
| `transform-origin` | ‏0 | ✅ |
| ‏keyframes מבוססי `translateX` | ‏2 — ראה F-6, F-8 | 🟡 מוכרעים |
| `letter-spacing` על עברית | ‏7 אתרי `tracking-wide` על תוויות עבריות | 🔴 F-4 |
| `scrollbar-gutter: stable` | **0** מופעים | 🟡 F-9 |
| טבלאות — מספרים/קוד/תאריכים LTR | מוסכמת `ltr-num` + `<bdi>` פרוסה נרחבות | ✅ |
| גרפים (SVG) | גרף ההכנסות בנוי RTL-first (‏`insetInlineStart` על ה-tip) | ✅ · ראה F-8 |
| ‏Portalled UI | ‏portal יחיד (`SidePanel`) נושא `dir="rtl"` מפורש; אין Radix/MUI | ✅ |

---

## ממצאים מדורגים

### P0 — אין
‏0 גלישות אופקיות, 0 שבירות פריסה, 0 שדות שמעוותים קלט. (המטריצה המלאה של #199,
שנמדדה על בדיוק העץ הזה, היא הראיה.)

### P1

| # | ממצא | קובץ | תיקון |
|---|---|---|---|
| **F-3** | **אין `dir="auto"` על אף שדה חופשי.** שם אורח באנגלית ("John Smith"), חיפוש באנגלית או הערה לטינית נצמדים לימין ומתפסקים הפוך. ‏16 השדות הטכניים (טלפון/אימייל/מספר כרטיס) כבר `dir="ltr"` — הפער הוא **שדות הטקסט החופשי** | ‏TopBar · ‏3 חיפושים · שמות/חברה/הערות ב-BookingPanel + EditReservationPanel · בעל הכרטיס ב-CardFields | ‏`dir="auto"` על שדות טקסט חופשי בלבד — **לא** על מספריים (ראה design fork D-R1) |

### P2

| # | ממצא | קובץ | תיקון |
|---|---|---|---|
| **F-1** | כפתור מתג המע״מ ממקם את הידית ב-`right-0.5`/`right-[22px]` — פיזי. ‏RTL נראה נכון במקרה, LTR היה נשבר | `PricingControls.tsx:269` | ‏`inset-s-*` לוגי — זהה פיקסל ב-RTL |
| **F-2** | `.cb-sheet` נעוץ ב-`left:0; right:0` פיזיים | `calendar-mobile.css:363` | ‏`inset-inline: 0` — סימטרי, זהה פיקסל |
| **F-4** | `tracking-wide` (‏letter-spacing) על תוויות **עבריות** — טיפוגרפיה עברית לעולם אינה מרווחת אותיות (סקיל, סעיף 5) | ‏PermissionsMatrix (‏th) · PermissionsByModule ×2 · StaffTable · SettingsShell · Sidebar | הסרה מהתוויות העבריות בלבד; ‏`tracking-tight` על "GuestHub" (לטיני) ו-`tracking-wide` על `<bdi class="ltr-num">` נשארים |
| **F-9** | אין `scrollbar-gutter: stable` — ב-RTL פס הגלילה יושב **משמאל**, והופעתו מזיזה את כל התוכן | הגולל הראשי (`main.thin-scroll`) + גוף המגירה (`.dw-bd`) | הוספה בשכבת responsive.css |

### P3 — נבדק, הוכרע, לא מתוקן (עם הנימוק)

| # | ממצא | הכרעה |
|---|---|---|
| **F-5** | `SidePanel` נפתח מהקצה השמאלי הפיזי (`left-0` + ‏framer `x:-100%`) | **מכוון.** החלטת מוצר (‏§7 במערכת העיצוב): המגירה נפתחת משמאל, ה-primary צמוד לקצה השמאלי. ‏framer-motion מקבל `x` פיזי בלבד — אין דרך לוגית. האפליקציה חד-כיוונית (RTL בלבד), וה-portal נושא `dir="rtl"` מפורש |
| **F-6** | `.cb-ghost` (‏`left:0` + transform פיזי) ו-`.cb-pop::after` (‏`left: var(--cb-caret)`) | **עקבי-פנימית.** הקומפוננטה מודדת פיקסלים פיזיים ב-JS ומציבה בעוגן פיזי — זוג פיזי-פיזי תקין. המרה חלקית ללוגי הייתה *שוברת* את הזוג |
| **F-7** | `responsive.css:313` — ‏`left:50% + translate` למרכוז אזורי מגע | **מכוון ומתועד** (‏ds-allow בקוד): ‏transform אינו מתהפך, לכן גם העוגן חייב להישאר פיזי. מרכוז זהה בשני הכיוונים |
| **F-8** | `.rev-tip` — עוגן **לוגי** (`insetInlineStart`) אך הזזה **פיזית** (`translateX(50%)`) | הצירוף נכון ב-RTL (נמדד). היה נשבר רק תחת LTR — מצב שאינו קיים במוצר. תיעוד בלבד; אין מוסיפים קוד מת למצב שלא קיים |
| **F-10** | ‏27 שאילתות `max-width` | **דפוס-על מכוון:** אפליקציית דסקטופ RTL קיימת שהשכבה הרספונסיבית שלה (‏#199) בנויה כ-overrides כלפי מטה, מתועד בראש `responsive.css`. היפוך ל-mobile-first היה משכתב את כל שכבת ה-CSS הוותיקה בסיכון רגרסיה מלא ו-0 רווח נמדד. חל על קוד חדש בלבד |
| **F-11** | ‏7 ‏TRUNCATED / ‏14 CLIPPED ברמת P1 במטריצה | ‏ellipsis מכוון על שמות בווידג'טים של הדשבורד (‏`overflow:hidden` + title מלא בפאנל) וחריגות 2–14px של תגי סטטוס — הוכרעו בסבבי #199 |
| **F-12** | אייקונים כיווניים ללא `rtl:-scale-x-100` | **לא ישים:** אין מצב LTR. כיווני החצים נבחרו ידנית נכון ל-RTL בכתיבה. הוספת flip הייתה הופכת אותם לשבורים |
| **F-13** | `MyTasksScreen` — ‏`border-r-4`, ‏`flex-row-reverse`, ‏radius לא תקני | מסך **קפוא** (‏STATE.md); ‏`check:design` כבר מדווח עליו. מחוץ להיקף בהחלטה קיימת |

## ‏Design forks שנרשמו ל-DECISIONS.md

- **D-R1:** ‏`dir="auto"` — על כל 197 השדות (כפרומפט) או רק על טקסט חופשי? על שדה
  מספרי, ‏dir="auto" עם ספרות בלבד נופל ל-LTR ומזיז את היישור שמאלה — שינוי מוצר
  נראה. **ברירת המחדל השמרנית:** טקסט חופשי בלבד.
- **D-R2:** ‏scrollbar-gutter — ‏`stable` על שני הגוללים הראשיים בלבד, לא גלובלית.
