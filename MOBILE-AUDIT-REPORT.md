# דוח התאמת מובייל ורספונסיביות — GuestHub

- **תאריך:** 2026-08-13 (סבב 5 — סנכרון מול `main` עדכני ואימות חוזר; סבב 4 היה פאנל עריכת ההזמנה)
- **ענף:** `fix/mobile-audit-2026-08-12` · worktree `~/worktrees/mobile-audit`
  - בסיס מקורי `main` @ `89d9d6e`; **מוזג לתוכו `origin/main` @ `946872d`** (‏#197) בסבב 5 — ראה §15
- **בייסליין להשוואה:** `~/worktrees/baseline-main-0813` — checkout נקי של **אותו** `origin/main` @ `946872d`
- **סביבת אימות:** האפליקציה מה-worktree על `127.0.0.1:3017` מול `guesthub-staging-db`
  (:5434, סכימה 083) + `guesthub-staging-auth` (:9989), משתמש `admin@ginot.co.il`.
  **אפס נגיעה בפרודקשן** — לא ב-DB, לא בפורט 3007, לא ב-pm2, ללא מיזוג וללא deploy.

> **על ניסוח:** אין כאן טענה ש"האתר רספונסיבי ב-100%". יש מדידות: מה נמדד, באיזה
> מנוע, ומה נשאר פתוח. כל טענה על iOS מסומנת כאימות **מנוע/מפרט** ולא כאימות על
> Safari אמיתי — ראה §9.

---

## 1. תקציר

העמודה האחרונה היא המצב אחרי **סבב 5** (הסנכרון מול `origin/main` @ `946872d`).
כל שורה בה נמדדה **מחדש** על העץ הממוזג — ראה §15. שתי שורות בלבד השתנו מסבב 4,
ושתיהן לטובה: כיסוי פאנל העריכה גדל מ-3 viewports ל-4, ו-`check:responsive` נספר
נכון. כל השאר שוחזר תו-בתו.

| מדד | לפני | סבב 1 | סבב 2 | סבב 3 | סבב 4 → **5** |
|---|---|---|---|---|---|
| תאים שנמדדו (route × viewport) | 286 | 286 | 391 | 391 | **391** (23 × 17) |
| ‏viewports | 13 | 13 | 17 | 17 | **17** |
| ממצאי **P0** (מטריצת pointer דק) | 82 | 0 | 0 | 0 | **0** מתוך 391 |
| ממצאי **P0** (מטריצת מגע) | — | — | 0 | 0 | **0** מתוך 66 |
| גלילה אופקית ברמת המסמך או אזור התוכן | יש | 0 | 0 | 0 | **0** |
| טבלאות רחבות עם ייצוג כרטיסים במובייל | 0/9 | 2/9 | 9/9 | 9/9 | **9/9** |
| פקדים מתחת ל-44×44 במגע | 833 | 833 | 0 | 0 | **0** |
| נקודות גבול מוסברות במלואן | — | — | ‏470/486 (ללא פירוט) | **492/492 · 0 החטאות**  ✓ |
| משטחים צפים — הרצות | — | 13/19 | 19/20 | 58/63 | **20/21** ב-390 (‏P-20 חסום) |
| ממצאי P0 בתוך משטחים ב-320px | — | — | לא נמדד | **0** (היו 13)  ✓ |
| שורות גלויות בלוח התעריפים ב-320×568 | — | — | לא נמדד | **8** (היו **0**)  ✓ |
| חסימת חדר בטלפון | בלתי אפשרית | בלתי אפשרית | בלתי אפשרית | **אפשרית** — פורטרייט ולנדסקייפ  ✓ |
| משטחים שלא נסגרו ב-Escape | — | 3 | 0 | 0 | **0** |
| ‏`check:responsive` | לא היה | 9 | 13 | 14 | **18 בדיקות** |
| **אלמנטים מחוץ ל-viewport בפאנל עריכת הזמנה (390)** | — | — | — | ‏183 | **0** |
| **מצבי פאנל עריכה שנמדדו** | — | — | — | 0 | **72** (4 vp × 18) · 0 כשלים · 0 לא-נמדדו |
| **פעולות פוטר מחוץ למסך** | — | — | — | 1 (‏"בטל הזמנה") | **0** |
| **סקרולרים אנכיים בגוף הפאנל** | — | — | — | לא נמדד | **1 בדיוק**, 0 מקוננים |
| **חפיפות אזורי מגע (‏elementFromPoint)** | — | — | — | ‏7 | **0** |
| **תאים שהוכיחו רינדור אמיתי** | — | — | — | לא נאכף | **391/391** · 0 error-boundary |

---

## 2. תשע הטבלאות — ייצוג כרטיסים ייעודי (9/9)

**העיקרון:** לא לשכפל JSX ולא ליצור נתיב לוגי שני. ארבע מהטבלאות הן CSS-grid, וכל
שורה נושאת התנהגות חיה — גרירה לשינוי סדר, מתגי הפעלה, כוכב ברירת מחדל עם שבע
סיבות השבתה נפרדות, אישור מחיקה דו-שלבי. לכן **השורה הופכת לכרטיס ב-CSS**, באותו
DOM: שינוי ה-markup הוא `data-label` אחד לתא, בלי state ובלי לוגיקה.

| # | מסך | שיטה | הפעולות שנשמרו |
|---|---|---|---|
| 1 | `/reservations` | `MobileRecordCard` | פתיחת פאנל, סטטוסים, יתרה |
| 2 | `/staff` | `MobileRecordCard` | עריכת עובד |
| 3 | `/guests` | `MobileRecordCard` | פתיחת פרופיל |
| 4 | `/permissions` | כרטיס לכל הרשאה + רשימת תפקידים | אותו `<Cell>`, אותו `toggle`, אותה מפת `pending` |
| 5 | סטטוסי הזמנה | `mcard` | סדר ▲▼, מתג, כוכב, עריכה, מחיקה דו-שלבית |
| 6 | אמצעי תשלום | `mcard` | ידית גרירה, מתג, מחיקה דו-שלבית |
| 7 | `/locks` | `mcard` | בחירה, שיוך, קודים, חשיפה, העתקה |
| 8 | מיפויי Beds24 | `mcard-table` | עריכה, שמירה, ביטול מיפוי |
| 9 | רשימת התבניות + הארכיון | `mcard` | תצוגה מקדימה, שכפול, אוטומציה, ארכיון, שחזור |

בנוסף הומרה גם טבלת שגיאות הסנכרון ב-`/channels` (‏`min-w-[560px]`) — לא הייתה
ברשימה, אבל גללה אופקית באותה מידה.

### שתי מלכודות שנמדדו ולא נוחשו

**‏1. סדר שכבות ה-CSS.** `status-settings.css`, `payment-methods.css` ו-`locks.css`
מצהירים **מחוץ** ל-`@layer components`. בקסקייד, כלל לא-מרובד מנצח כל כלל מרובד
ללא קשר לספציפיות — ולכן `.mcard-rows > .mcard-row` (0,2,0) הפסיד ל-
`.ws-tbl { min-width: 830px }` (0,1,0). נמדד: השורה נשארה רשת של שבעה מסלולים
והמקטע עדיין גלל 830px בתוך כרטיס 336px. הבלוק הועבר להיות לא-מרובד — וזה גם מה
שמאפשר לו לדרוס `overflow-x-auto` של Tailwind, כי utilities הן שכבה גם הן.
‏`check:responsive` #13 נועל את סדר השכבות הזה.

**‏2. סגנון inline.** רשימת הארכיון הצהירה `minWidth: 760` ב-`style`. סגנון inline
מנצח כל גיליון סגנון, ולכן הרצפה הזו הייתה שורדת את פריסת הכרטיסים. חולץ ל-`.gc-arch`.

---

## 3. אזורי מגע — 44×44 בלי להגדיל את הממשק

בייסליין מדוד: **833 פקדים מתחת ל-44×44**, רובם הגדלים הקנוניים של §4 —
`.icon-btn` 36×36 (‏247 מופעים), `.btn-sm` בגובה 36, `.chip` 28, `.cb-nav` 36×36.
הגדלים האלה **הם** מערכת העיצוב, ו-`check:design` אוכף אותם כקבוצה סגורה.

**הפתרון:** כל פקד שומר על גודלו המצויר ומקרין `::after` שקוף וממורכז עד 44×44,
רק תחת `pointer: coarse`. הכלל מגביל את עצמו — `width/height: 100%` עם רצפת 44px
פירושו שפקד שכבר ≥44 מקבל `::after` בגודלו בדיוק: אפס הרחבה, אפס חפיפה.
גנרי דרך `:where()` (ספציפיות 0) ולא רשימת מחלקות, כי רשימה מנויה **פספסה** 325
תיבות סימון בהרשאות, כל Switch, כל כפתורי הסגמנט ואת שדות החיפוש.

**‏`:disabled` ו-`[aria-disabled="true"]` לא מקבלים אזור מגע כלל** — פקד מושבת נשאר מת.

**חפיפות טופלו במדידה, לא בהנחה.** איפה ששני פקדים קטנים היו צמודים מדי, ה-gap
הורחב למינימום שמבטל את החפיפה (‏44 = 36+8, ‏44 = 28+16). שני חצי הסדר הזעירים
(‏22×16 במרחק 2px) קיבלו קופסה **אמיתית** של 44 — הרחבה מרכזית לא יכולה לעזור שם,
וקופסה אמיתית נלקחת בחשבון בפריסה ולכן חפיפה בלתי אפשרית מבנית. האייקונים לא שונו.

### הבאג שהיה בתיקון עצמו

הגרסה הראשונה עיגנה את ה-`::after` ב-`inset-inline-start: 50%` עם
`translate: -50% -50%`. במסמך RTL זה נפתר ל-`right: 50%`, ו-`translate` הוא פיזי
ו**לא** מתהפך איתו — כך שאזור המגע נדחף ברוחב מלא **שמאלה** מהפקד במקום למרכזו.

זה לא נתפס בסבב הראשון כי המדידה **חישבה** איפה ה-`::after` אמור להיות והסיקה
"‏0 מתחת ל-44" — מדידה שמאשרת את עצמה. בדיקת פגיעה אמיתית
(`document.elementFromPoint`) על נקודה 21px מעל מרכז כפתור אייקון 36px החזירה
אלמנט אחר לגמרי: המטרה של 44px לא הייתה קיימת במקום שבו אגודל נוחת.

התיקון הוא מרכוז ב-`left`/`top` **פיזיים** — transform לא יכול לשקף, ולכן גם העוגן
שלו לא צריך. `check:responsive` #10 אוסר עכשיו במפורש את הצורה השבורה.

### חשבון מלא של נקודות הגבול

‏"470/486" בסבב הקודם היה אגרגט בלי פירוט, וזה בדיוק סוג המספר שמסתיר פגם. עכשיו
כל נקודה שאינה פוגעת **מסווגת**, והסיווגים נבדקים לפי הסדר (הראשון שמתאים זוכה):

```text
14 מסלולים · 123 פקדים מתחת ל-44×44 · 4 נקודות גבול לכל פקד (±21px)

נקודות גבול שנבדקו        492
  פגיעות תקפות            467   (94.9%)
  מחוץ ל-viewport           0
  נחתכות ע"י מכל גלילה      0
  מכוסות ע"י שכבה צפה       0
  שייכות לפקד שכן          25   (5.1%)  ← ההחרגה היחידה
  ---------------------------------
  החטאות אמיתיות            0
  סה"כ מוסבר             492/492 ✓

מרכז הפקד בבעלותו        123/123   (0 centre-miss)
מטרות מתות (0 מ-4)          0
משתרע על 44px בציר כלשהו 123/123
```

**כל 25 ההחרגות הן צורה אחת:** צ'יפים בגובה 28px בשורות עוקבות. הנקודה 21px מתחת
למרכז צ'יפ נופלת בתוך הצ'יפ שבשורה מתחתיו — שהוא **פקד אמיתי בזכות עצמו**. שני
פקדים אינם יכולים להחזיק באותו פיקסל; מי שנמצא מלמעלה זוכה. זו לא החטאה של אזור
המגע אלא הגבול הטבעי בינו לבין השכן, ולכן **כל 123 הפקדים עדיין משתרעים על 44px
מלאים בציר אחד לפחות** (עבור הצ'יפים — האופקי).

**מה נוסה ונדחה על סמך מדידה:** הכללה של `row-gap: 16px` לכל שורת צ'יפים דרך
`:has(> .chip)`. היא אכן הרימה את /staff ו-/rate-plans מ-8px ל-16px — ומספר
הנקודות שבבעלות שכן **לא זז** (25 לפני, 25 אחרי), כי ההצטלבויות שנותרו הן בין
צ'יפים ב**כרטיסים סמוכים**, שאין row-gap ששולט בהם. לרווח כל שורת צ'יפים באפליקציה
במגע ללא רווח מדוד — זו בדיוק ניפוח הצפיפות שהמעבר הזה אסור לו לעשות. הכלל הוחזר
לשתי השורות שנמדדו.

**נמדד אחרי התיקון (ראה §10.3): 0 החטאות אמיתיות, 0 מטרות מתות, 0 חפיפות שגוזלות מרכז.**

---

## 4. משטחים צפים — 21 משטחים × 3 viewports

הסבב הקודם הריץ את ההליכה ב-390×844 בלבד, וזה הסתיר פגמים אמיתיים. עכשיו כל משטח
נפתח בשלושה viewports: **390×844**, **320×568** (הצר ביותר) ו-**844×390** (טלפון
בלנדסקייפ — קצר, ורחב מ-`md`).

| viewport | נפתחו | ‏P0 | ‏Escape נכשל | רחב מה-viewport | גבוה מה-viewport | לא נפתחו |
|---|---|---|---|---|---|---|
| ‏390×844 | 20/21 | **0** | 0 | 0 | 0 | P-20 |
| ‏320×568 | 20/21 | **0** | 0 | 0 | 0 | P-20 |
| ‏844×390 | 18/21 | **0** | 0 | 0 | 0 | P-20, P-15M, P-35 |

‏**58 מתוך 63 ההרצות נפתחו. חמש שלא — כולן מוסברות, אף אחת לא "דולגה":**

| לא נפתח | סיבה |
|---|---|
| ‏P-20 ‏(×3) | **חסם נתונים.** `select count(*) from guesthub.ttlock_locks` על staging = **0**. ה-drawer נפתח רק אחרי בחירת שורת מנעול, ואין שורות. נשאר **OPEN** — לא נוצרו נתוני מנעולים מלאכותיים כדי שהבודק יעבור |
| ‏P-15M ‏(844×390) | נקודת הכניסה של **עץ המובייל**, `md:hidden`. ב-844 רץ עץ הדסקטופ, ושם אותו פאנל נפתח דרך `.cb-touch-close` ודרך P-15 — שניהם נמדדו באותה הרצה |
| ‏P-35 ‏(844×390) | ה-bottom sheet הוא רכיב של עץ המובייל בלבד; ב-844 הלוח הוא גריד הדסקטופ, ותפקידו מכוסה ע"י EditReservationPanel שנמדד |

### שלוש פעמים שבהן הבודק היה השבור, לא האפליקציה

הבקשה אמרה במפורש: אין לסמן משטח כ-PASS רק כי ה-walker לא מצא את הטריגר. שלושה
מקרים כאלה נבדקו עד הסוף:

1. **‏P-01 ב-844×390 — "לא נגיש".** שני שלבי השרשרת היו קיימים וגלויים. הבעיה:
   מעל `md` הסיידבר **כבר פתוח**, ולחיצה על ההמבורגר *סגרה* אותו. השלב סומן
   כתנאי: מדלגים עליו כשהסיידבר כבר על המסך.
2. **‏P-02 / P-14 ב-844×390.** הטריגרים הצביעו על כרטיס המובייל, שקיים ב-DOM אבל
   `display: none` מעל `md`. נוספו החלופות של עץ הדסקטופ (`table.rv-tbl tbody tr`,
   `.gl-rowg.rl-trow`).
3. **‏P-23 ‏CellDetailPanel.** ‏`.rg-price` תפס את מחיר הדוגמה ב**אגדה**, שמכוסה
   ע"י `.rg-wrap`. אחרי התיקון ל-`.rg-cell.editable .rg-price` המשטח עדיין לא
   נפתח — וזה הוביל לפגם האמיתי החמור ביותר בסבב הזה (§4.1).

### 4.1 · לוח התעריפים היה בגובה אפס

‏`.rg-scroll` הוא `flex: 1` עם `min-height: 0` בתוך `.rg-wrap { height: 100% }`.
כלומר הלוח מקבל את מה שנשאר אחרי הסרגל, שורת הסינון והאגדה. נמדד:

| viewport | גובה `.rg-scroll` | שורות גלויות |
|---|---|---|
| ‏1280×800 | 440px | 9 |
| ‏390×844 | 138px | 3 |
| ‏375×812 | 87px | 1 |
| **‏320×568** | **0px** | **0** |
| **‏844×390** | **0px** | **0** |
| **‏568×320** | **0px** | **0** |

בשלושת האחרונים מסך התעריפים לא הציג **ולו שורת מחיר אחת**, ולא הייתה שום גלילה
שמגיעה אליה. המטריצה לא ראתה את זה: קופסה בגובה 0 לא גולשת משום דבר, לא נחתכת,
ולא מייצרת אף ממצא. רק ניסיון לפתוח משטח *דרך* תא בלוח חשף אותה.

**התיקון:** מתחת ל-`md` או בגובה viewport קטן, `.rg-wrap` מפסיק להיות כבול לגובה
ל-`.rg-scroll` יש רצפה של 6 שורות ותקרה של `70dvh` (התקרה שומרת על הגלילה הפנימית
ועל כותרת הימים הדביקה). אחרי: 8 שורות ב-320×568, 6 ב-844×390, ו-**הדסקטופ לא זז**
(‏1280×800 נשאר 440px / 9 שורות).

### Escape

שני משטחים לא ענו ל-Escape כלל: הגיליון התחתון ו-popover הסטטוס בחדרים. שניהם
מאזינים עכשיו **בשלב ה-capture** ועוצרים את האירוע — הגיליון יכול לשבת מעל הלוח,
ובלי עצירה Escape אחד היה סוגר את שניהם. הגיליון מעביר פוקוס פנימה בפתיחה ומחזיר
אותו לטריגר בסגירה. גלילה, נגיעה וסקרין לא שונו. ‏`check:responsive` #12 נועל.

**מלכודת מדידה שתוקנה:** ה-SidePanel יוצא דרך `AnimatePresence` עד 1.2 שניות, והמתנה
של 700ms קראה פאנל באמצע יציאה כ"לא נסגר". שלושת ה"כשלונות" בסבב 1 היו ארטיפקט של
הבודק, לא פגם.

---

## 5. ‏`/reservations/[id]/print`

נבדק עם **מזהה הזמנה אמיתי מ-staging**, שנקרא מה-DB בקריאה בלבד
(`select id from guesthub.reservations order by created_at desc limit 1`) — לא הומצא.
נמדד ב-390×844, ‏768×1024, ‏1280×800 ו-844×390 (landscape): טעינה תקינה בכל ארבעה,
**0 P0, 0 P1**, ללא גלילה אופקית.

---

## 6. ‏Landscape של טלפון

נוספו למטריצה ארבעה viewports שבהם **הגובה** הוא האילוץ ולא הרוחב:
`568×320 · 667×375 · 844×390 · 932×430`. גובה של 375px צריך להכיל סרגל עליון sticky
בן 64px, footer של מגירה, ועם מקלדת וירטואלית כמחצית מזה.

הכיסוי חל על כל 23 המסלולים — הניווט, מסכי ההזמנה, הלוח, הטפסים והכרטיסים החדשים —
‏**92 תאים, 0 ממצאי P0**. המשטחים הצפים נמדדים עכשיו גם ב-844×390 (§4), ונקודת
הכניסה לחסימת חדר נבדקה ב-740×360 וב-844×390 (§11).

**ה-landscape הוא שגילה שני דברים שהפורטרייט לא יכול היה לגלות:**

1. **לוח התעריפים בגובה אפס** ב-844×390 וב-568×320 (§4.1) — פגם שנובע מגובה, לא מרוחב.
2. **חסימת חדר לא הייתה נגישה גם אחרי התיקון הראשון.** טלפון בלנדסקייפ הוא 844px
   רחב — כלומר **מעל `md`** — ולכן מקבל את עץ הדסקטופ, שבו החסימה נפתחת רק בקליק
   ימני. תוקן ב-`.cb-touch-close`, שמתגלה לפי `pointer: coarse` ולא לפי רוחב.

---

## 7. תיקונים עיקריים בסבב הזה

| # | תיקון |
|---|---|
| 1 | ‏7 טבלאות → כרטיסי מובייל (+ טבלת שגיאות הסנכרון) |
| 2 | בלוק row→card הועבר מחוץ ל-`@layer components` — אחרת אינו פועל כלל |
| 3 | ‏`minWidth: 760` inline בארכיון חולץ ל-CSS |
| 4 | אזורי מגע 44×44 גנריים ב-`pointer: coarse`, שקופים, לא על פקד מושבת |
| 5 | הרחבת gap היכן שאזורי המגע חפפו; ‏`.ws-ord` קיבל קופסה אמיתית |
| 6 | ‏`Escape` לגיליון התחתון ול-StatusPopover — capture + החזרת פוקוס |
| 7 | ‏`.dw-hd` נעטף — כותרת חלון ההזמנה נמדדה 428px בתוך מגירה של 390px |
| 8 | ‏`.card-hd` נעטף — ב-320px כותרת המקטע נמעכה ל-24px ונחתכה |
| 9 | **סבב 3:** נקודת כניסה מפורשת לחסימת חדר בשני העצים (§11) |
| 10 | **סבב 3:** ‏`.rg-scroll` קיבל רצפת 6 שורות — היה 0 שורות בשלושה viewports (§4.1) |
| 11 | **סבב 3:** ‏7 קופסאות שלא נכנסות ב-320px בתוך פאנלים (§4) |
| 12 | **סבב 3:** הרשאת `auth.users` הפכה לאינווריאנט ב-`check:db-isolation` (§8/O-4) |
| 13 | ‏`check:responsive` 9 → **14** בדיקות |

---

## 8. ‏OPEN ISSUE

### O-1 · ‏WebKit לא רץ — חסרות תלויות מערכת

‏`webkit-2311` הותקן בהצלחה, אבל ההרצה נחסמת:

```
Host system is missing dependencies to run browsers.
  sudo apt-get install libwayland-server0 libxml2
```

זהו שינוי ברמת המערכת על מארח משותף שמריץ פרודקשן, ודורש `sudo`. **לא בוצע** —
נדרש אישור מפורש. עד אז כל הבדיקות רצו ב-Chromium בלבד.

### O-2 · ‏P-20 — ‏drawer הקודים ב-`/locks` לא נפתח

**נבדק שוב ב-2026-08-13 — עדיין חסום.**

```sql
select count(*) from guesthub.ttlock_locks;   -- 0
```

- **החסם המדויק:** אין ולו שורת מנעול אחת ב-staging.
- **נתיב הטריגר:** `/locks` → `.lk-row button[aria-label^="בחירת"]` (בחירת שורת
  מנעול) → `button:has-text("החלפת קוד דירה")`. השלב הראשון אינו קיים בלי נתונים,
  ולכן השני לעולם לא מגיע.
- **מה לא אומת:** רוחב/גובה ה-drawer מול ה-viewport, נעילת הגלילה, מיקום ה-footer,
  התנהגות Escape, והחזרת הפוקוס — עבור **המשטח הזה בלבד**. מסך `/locks` עצמו נמדד
  ב-17 viewports ‏(0 ממצאי P0), והמרת השורות לכרטיסים אומתה ב-320 וב-390.
- **מה לא נעשה:** לא הוזרקו נתוני מנעולים סינתטיים כדי שהבודק יעבור. רשומת מנעול
  היא נתון תפעולי (מזהה TTLock, סודות מוצפנים) והמצאתה הייתה יוצרת מדידה של משהו
  שאינו קיים — בדיוק הכשל שהדוח הזה מתעד במקומות אחרים.
- **הפתרון:** רשומת מנעול אמיתית ב-staging, או הרצה מול סביבה שיש בה מנעולים.

### ~~O-3~~ · ‏ClosurePanel במובייל — **נסגר**

ראה §11. נוספו שתי נקודות כניסה מפורשות לאותו פאנל, ואומתו חי.

### ~~O-4~~ · ‏`/staff` — **נסגר, כולל פרודקשן**

ב-staging לא הייתה ל-`guesthub_app` הרשאה על סכימת `auth`, ולכן `/staff` החזיר
error boundary. **כל מדידה של המסך הזה לפני התיקון מדדה דף שגיאה.**

**פרודקשן נבדק בקריאה בלבד ואין בו פער.** מה שהמסלול צורך הוא בדיוק
`LEFT JOIN auth.users au ON au.id = u.auth_user_id` עבור `au.last_sign_in_at`
(`src/app/(dashboard)/staff/page.tsx:27`), כלומר `USAGE` על הסכימה ו-`SELECT`
עמודתי על `id` ו-`last_sign_in_at`. הנמדד בפרודקשן כ-`guesthub_app`:

```text
has_schema_privilege(auth, USAGE)            = t
has_table_privilege(auth.users, SELECT)      = f      ← נכון: אין הרשאה ברמת טבלה
has_column_privilege(id, SELECT)             = t
has_column_privilege(last_sign_in_at, SELECT)= t
attacl(auth.users) = id, last_sign_in_at, created_at → guesthub_app
SELECT count(*) FROM guesthub.users u LEFT JOIN auth.users au … = 9   (רץ)
```

זה **מינימום ההרשאה** — שלוש עמודות, בלי `encrypted_password` ובלי עמודות הטוקן.
לא נדרש ולא בוצע שינוי הרשאות בפרודקשן.

**התיקון בריפו.** ההרשאה כן מוגדרת ב-`db/roles/roles.sql` §3b, אבל היא עטופה
ב-`IF EXISTS (auth schema)`. בסביבה חדשה `roles.sql` רץ **לפני** ש-GoTrue יצר את
הסכימה, התנאי שקרי, וההרשאה לא ניתנת לצמיתות ובשקט — בדיוק מה שקרה ב-staging.
נוספו: `RAISE WARNING` מפורש ב-`roles.sql` כשהסכימה חסרה, ושתי טענות ב-
`check:db-isolation` שמאמתות את מצב הקצה — שההרשאה קיימת, ושהיא **נשארת עמודתית**
(‏`GRANT SELECT` ברמת טבלה נחסם, כי הוא חושף את סיסמת ה-hash).

‏staging יושר גם הוא למינימום: ה-`GRANT SELECT ON ALL TABLES IN SCHEMA auth` שניתן
בסבב הקודם בוטל, והוחלף בשלוש העמודות. נמדד אחרי: ה-JOIN של `/staff` רץ,
ו-`select encrypted_password` מחזיר `permission denied for table users`.

### ~~O-5~~ · המשטחים הצפים — **נסגר**

ההליכה על המשטחים הצפים הורצה בשלושה viewports (‏390×844, ‏320×568, ‏844×390
landscape) ולא באחד. ראה §10.4.

### ~~O-9~~ · פאנל עריכת ההזמנה במובייל — **נסגר בסבב 4**

צילומי מכשיר אמיתי הראו את גוף ההזמנה נדחף הצידה ואת פס הגלילה הפנימי. נמדד
ונסגר — ראה §14. ארבעה שורשים נפרדים, ארבע טענות רגרסיה חדשות.

### O-6 · צפיפות הנתונים ב-staging נמוכה מפרודקשן

‏83 הזמנות, 15 חדרים, 7 משתמשים, 0 מנעולים. **המדידות תקפות למבנה, לא לנפח.**

### ~~O-8~~ · בנייה בתוך worktree מפיקה CSS חסר — **נסגר בסבב 5, הסיבה אותרה**

> **עדכון סבב 5 (‏§15.5):** התסמין **לא משוחזר**. `main` נקי והענף הממוזג נבנו
> שניהם `exit 0` עם צ'אנק CSS מלא (‏41.4kB ו-42.9kB), והשכבה הרספונסיבית אומתה
> **בתוך ה-CSS הבנוי**. הסיבה האמיתית: `next dev` רץ באותו worktree וכותב לאותו
> `.next`; בנייה במקביל מוחקת מתחתיו את הצ'אנקים, ומכאן ההפניה ל-chunk שאינו
> קיים. עוצרים את שרת ה-dev לפני `pnpm build` והתסמין נעלם. הניתוח המקורי, כפי
> שנרשם אז, נשאר להלן.

כל המדידות בדוח הזה — בסבב הזה ובקודמים — רצו על **שרת ה-dev** של Next
(`next dev --turbopack`, פורט 3017). זו החלטה, ויש לה סיבה שנמדדה:

ניסיתי לעבור לבנייה אמיתית (`pnpm build` + `next start`) כדי לייצב את ההרצות, וקיבלתי
תוצאות שנראו כמו קטסטרופה — `.app-shell` בגובה 17,164px, כל טבלה רחבה מוצגת בגרסת
הדסקטופ שלה, 90 ממצאי P0. הבדיקה הראתה שזו לא האפליקציה:

```text
ה-HTML מפנה ל-  /_next/static/chunks/e2f9839b63cd8083.css   ← לא קיים על הדיסק
על הדיסק יש     /_next/static/chunks/14d14014ab57d064.css   384KB, 42 @media, mcard ✓
                /_next/static/chunks/a6cda61721c650c4.css     1.8KB
בדפדפן:  mediaRules=3   mcardRules=0
```

הגיליון הראשי פשוט לא נטען — הבנייה מפיקה הפניה ל-chunk שהיא לא כותבת. חזר גם אחרי
`rm -rf .next` ובנייה נקייה. חשוד עיקרי: האזהרה שהבנייה עצמה מדפיסה —
`Next.js inferred your workspace root ... selected /home/ubuntu/pnpm-lock.yaml`,
כלומר שורש ה-workspace נבחר מחוץ ל-worktree.

**זה לא נגרם מהשינויים בענף** (הם CSS ו-TSX בלבד), **ופרודקשן לא מושפע**: פורט 3007
מגיש שני קבצי CSS שקיימים, והוא נבנה דרך `npm run deploy:prod` ולא דרך בנייה
ידנית ב-worktree. אבל זו מלכודת אמיתית לכל מי שיבנה בעץ עבודה — ולכן היא מתועדת
כאן ולא נבלעת. **לא נגעתי בהגדרות הבנייה**; זה מחוץ להיקף המעבר הזה.

### O-7 · הבודק מודד גיאומטריה, לא איכות

הוא לא קובע אם הכרטיס שנבנה הוא מה שפקיד קבלה באמת רוצה לראות בטלפון. אין תחליף
לסקירה אנושית של המסכים החדשים.

---

## 9. טענות iOS — סיווג מדויק

| טענה | סטטוס האימות |
|---|---|
| ‏`100dvh` פותר את חיתוך סרגל הכתובת | **מפרט + Chromium.** לא נבדק על Safari |
| ‏`env(safe-area-inset-*)` נכנס לתוקף עם `viewportFit: cover` | **מפרט.** לא נבדק על מכשיר עם notch |
| ‏17px מונע זום בפוקוס | **מפרט + מדידת computed style** בהקשר `pointer: coarse` ‏(17/17/17). הזום עצמו לא נצפה |
| אזורי מגע 44×44 | **נמדד** גיאומטרית ב-Chromium בהקשר מגע |
| ‏`Escape`, פוקוס, נעילת גלילה | **נמדד** ב-Chromium |

אין בדוח הזה שום טענה שמשהו "אומת על iOS Safari".

---

## 10. אימות סופי

### 10.0 · המכנה — איך 391 מחושב

‏`391` אינו אגרגט. הוא מכפלה של שתי רשימות שכתובות בקוד ההרצה
(`scratchpad/audit.mjs`), וכל מי שיריץ את אותו קובץ יקבל את אותו מכנה:

**‏17 viewports** — 13 ראשיים + 4 landscape של טלפון:

| קבוצה | ערכים |
|---|---|
| טלפון portrait | ‏320×568 · 360×800 · 375×812 · 390×844 · 414×896 · 430×932 |
| ביניים | ‏480×800 · 600×900 |
| טאבלט | ‏768×1024 · 820×1180 |
| דסקטופ | ‏1024×768 · 1280×800 · 1440×900 |
| **טלפון landscape** | ‏568×320 · 667×375 · 844×390 · 932×430 |

**‏23 מסלולים** — `R-01`…`R-22` ועוד `R-24` (‏`R-23` נמחק מהרשימה):

```text
/dashboard  /calendar  /rates  /rate-plans  /reservations  /guests  /rooms
/housekeeping  /maintenance  /locks  /channels  /settings  /staff  /permissions
/communications/{templates,automations,history,channels,archive}
/login (הקשר אנונימי נפרד)   /housekeeping/my-tasks   404
/reservations/<id>/print  (מזהה אמיתי מ-staging, לא מומצא)
```

```text
מכנה המטריצה הראשית = 23 מסלולים × 17 viewports = 391 תאים   (pointer דק)
מתוכם landscape     = 23 מסלולים ×  4 viewports =  92 תאים
מטריצת המגע         = 22 מסלולים ×  3 viewports =  66 תאים   (hasTouch)
משטחים צפים         = 21 משטחים  ×  3 viewports =  63 הרצות
בדיקת פגיעה         = 14 מסלולים × 1 viewport, כל פקד <44px × 4 נקודות גבול
```

**מה מוחרג במכוון.** מטריצת המגע מדלגת על `/login` (‏R-20) כי היא רצה בהקשר
מאומת — במסלול הזה ה-middleware מחזיר את המשתמש לדשבורד, ולכן היא נמדדת בהקשר
אנונימי נפרד במטריצה הראשית בלבד. אין שום מסלול או viewport אחר שמדולג.
‏`sea-tower` (פורט 3005, ריפו נפרד) מחוץ להיקף לפי החלטת המשתמש, ולכן פריטי
האתר הציבורי ב-Stage 2 סומנו `NOT APPLICABLE` ולא `PASS`.

**סבב 1 היה 286 תאים** (‏22 × 13) — בלי מסלול ההדפסה ובלי ארבעת ה-landscape.
ההפרש הוא בדיוק `23×17 − 22×13 = 391 − 286`.

**להרצה חוזרת:**

```bash
cd <scratchpad>
set -a; . ./audit-user.env; set +a          # AUDIT_BASE_URL/EMAIL/PASSWORD (staging בלבד)
AUDIT_RESERVATION_ID=$(cat rid.txt) AUDIT_OUT=findings.json node run-audit.mjs
```

### 10.1 · מטריצה מלאה — ‏pointer דק (עכבר)

‏23 מסלולים × 17 viewports = **391 תאים** (‏§10.0).

```text
391/391 תאים נטענו · 0 שגיאות ניווט · 0 שגיאות JS · 0 שגיאות probe · 0 ממצאי P0
document.scrollWidth > clientWidth  ב-0 תאים מתוך 391
main.scrollWidth   > clientWidth    ב-0 תאים מתוך 391
מתוכם 92 תאי landscape של טלפון — 0 P0
```

ההרכב שנותר (הכול P1): 1,139 TAP-SMALL · 250 INPUT-ZOOM · 14 CLIPPED · 9 TRUNCATED.
**שתי הראשונות אינן ממצאים בהקשר הזה** — הן נמדדות ב-pointer דק, שבו כלל ה-44px
וכלל ה-17px **במכוון** אינם חלים. עכבר לא צריך 44px. המדידה הרלוונטית היא 10.2.

### 10.2 · מטריצה בהקשר **מגע** (`hasTouch`)

‏22 מסלולים × 3 viewports (‏390×844, ‏844×390 landscape, ‏320×568) = **66 תאים**.

```text
0 ממצאי P0 · 0 INPUT-ZOOM · 0 שגיאות ניווט · 166 TAP-SMALL (גודל מצויר) · 26 CLIPPED ברמת P1
```

‏`INPUT-ZOOM = 0` הוא האימות שכלל ה-17px באמת נדלק במגע — בסבב הקודם הוא היה שבור
בשקט (‏select ו-textarea הפסידו בספציפיות) ואיש לא היה יודע בלי המדידה הזו.

‏`TAP-SMALL = 166` הוא **הגודל המצויר**, שנשמר בכוונה. אזור המגע האפקטיבי נמדד בנפרד:

### 10.3 · בדיקת פגיעה אמיתית (ground truth) — חשבון מלא

במקום לחשב איפה אזור המגע אמור להיות, נשאל הדפדפן מה נמצא בנקודה. כל נקודה שאינה
פוגעת מסווגת; אין נקודה שנשמטת בשקט:

```text
14 מסלולים · 123 פקדים מתחת ל-44×44 · 4 נקודות גבול לכל פקד (±21px)

נקודות גבול שנבדקו        492
  פגיעות תקפות            467
  מחוץ ל-viewport           0
  נחתכות ע"י מכל גלילה      0
  מכוסות ע"י שכבה צפה       0
  שייכות לפקד שכן          25
  ---------------------------------
  החטאות אמיתיות            0     ← קריטריון הקבלה
  מוסבר                  492/492 ✓

מרכז הפקד בבעלותו        123/123  (0 חפיפות שגוזלות מרכז)
מטרות מתות                  0
משתרע על 44px בציר         123/123
```

‏25 ההחרגות מפורטות ומנומקות ב-§3 — כולן צ'יפים בשורות עוקבות, וכולן מותירות לפקד
‏44px מלאים בציר האופקי.

**כך התגלה באג שהכנסתי בעצמי:** המדידה הקודמת חישבה איפה ה-`::after` אמור להיות
והסיקה "0 מתחת ל-44" — מדידה שמאשרת את עצמה. בדיקת הפגיעה גילתה
`edge(+21px)-hit = 0`: אזור המגע ישב **ליד** הפקד ולא עליו, כי
`inset-inline-start: 50%` נפתר ל-`right: 50%` ב-RTL בעוד ש-`translate` נשאר פיזי.

### 10.4 · משטחים צפים — 21 × 3 viewports

```text
390×844   20/21 נפתחו   P0=0   Escape נכשל=0   רחב=0   גבוה=0
320×568   20/21 נפתחו   P0=0   Escape נכשל=0   רחב=0   גבוה=0
844×390   18/21 נפתחו   P0=0   Escape נכשל=0   רחב=0   גבוה=0
          ------------------------------------------------------
          58/63 הרצות · 5 שלא נפתחו, כולן מוסברות ב-§4
```

### 10.5 · תשע הטבלאות + טבלת שגיאות הסנכרון

נמדד ב-320px וב-390px: `doc == client` ו-`main == client` בכל אחת, **0 ממצאי P0**.
הגוללים האופקיים שנותרו בעמודים האלה:

| גולל | מה זה | הכרעה |
|---|---|---|
| `nav.gc-tabs` (‏615px בתוך 264/334) | סרגל הטאבים של התקשורת | **מכוון.** סרגל טאבים, לא טבלת נתונים. גלילה אופקית בו היא הדפוס המתועד בפרויקט |
| `div.card.mcard-wrap` ב-`/channels` ב-320px (‏269 בתוך 266) | עוטף כרטיסי המיפוי | **3 פיקסלים.** אין תוכן מוסתר (‏0 ממצאי CLIPPED), המסמך ואזור התוכן שניהם 320/320. נרשם כאן במדויק ולא מוצג כאפס |

אף אחת מתשע הטבלאות אינה נשענת על גלילה אופקית בטלפון.

### 10.6 · ‏suite מלא — ענף מול main נקי

שתי ההרצות באותה מכונה, באותה סביבה, זו אחרי זו. **המספרים כאן הם של סבב 5** —
מול `origin/main` @ `946872d`, לא מול הבסיס הישן `89d9d6e`. (‏בסבב 4 היו 84/8/1
מתוך 93 מול 85/8/1 מתוך 94; ‏#197 הוסיף שומר אחד לשני הצדדים.)

| | main נקי @ `946872d` (`~/worktrees/baseline-main-0813`) | הענף אחרי המיזוג |
|---|---|---|
| עברו | 85 | **86** |
| נכשלו | 8 | **8** |
| לא ניתן להרצה | 1 | **1** |
| סה"כ | 94 | **95** |

**רשימות הכישלון זהות תו-בתו:**

```text
main   : design, effective-state, guard-roots, guest-communications-db,
         hydration-browser*, inventory, rate-grid, sellability, supply-chain
branch : design, effective-state, guard-roots, guest-communications-db,
         hydration-browser*, inventory, rate-grid, sellability, supply-chain

רק בענף (רגרסיות) : (ריק)
רק ב-main (תוקנו)  : (ריק)
```

`*` `check:hydration-browser` = `cannot-run` (חסרים `HYDRATION_BASE_URL/EMAIL/PASSWORD`), בשתיהן.

**‏0 רגרסיות.** ההפרש היחיד בין 94 ל-95 הוא `check:responsive`, הבדיקה החדשה — והיא עוברת
(‏18/18). שמונה הבדיקות שנכשלות בשתיהן נכשלו כבר לפני הענף הזה; `check:design` נכשל על
הפרות שכולן ב-`MyTasksScreen.tsx` (מסך קפוא), אף אחת מהן לא נגעה בשינויים כאן.

`typecheck` ו-`lint` הורצו על שני העצים: **0 שגיאות ו-34 אזהרות בשניהם** — הענף לא
מוסיף ולו אזהרה אחת.

### 10.7 · ‏git diff

```text
מול origin/main @ 946872d:
54 קבצים · +3,487 / −159
32 tsx · 11 css · 5 mjs · 2 ts · 2 md · 1 sql · 1 json
```

הקבצים שאינם CSS/TSX/דוח, אחד-אחד:

| קובץ | למה |
|---|---|
| `scripts/check-responsive.mjs` | השומר החדש (‏18 טענות) |
| `scripts/check-db-isolation.mjs` | שתי טענות על הרשאת `auth.users` (‏§8/O-4) |
| `scripts/check-calendar-ui.mjs` · `check-rates-ui.mjs` · `check-reservations-ui.mjs` | עודכנו כדי לעקוב אחרי הקוד שזז (‏316 עבר ל-`lib/popover.ts`, ‏`w-[60vw]` הפך ל-`lg:w-[60vw]`, ‏2→3 אתרי קריאה) |
| `src/lib/breakpoints.ts` · `src/lib/popover.ts` | מקור אמת יחיד ל-`md` ולרוחב ה-popover, במקום ערכים משוכפלים ביד |
| `db/roles/roles.sql` | `RAISE WARNING` כשסכימת `auth` חסרה |
| `package.json` | רישום `check:responsive` |

נסרק שורה-שורה: **אין שינוי שאינו מובייל / RTL / השומרים שלהם.** אין שינוי בלוגיקה
עסקית, בתמחור, ב-API, בסכימה, ב-copy, בצבעי מותג או בפונט. הקובץ עם הכי הרבה שורות
שהשתנו מחוץ ל-CSS הוא `TaskDispatchBoard.tsx`, ושם **כל** השינויים הם
`text-right` → `text-start`.

---

## 11. חסימת חדר במובייל — הפער התפקודי נסגר

**מה היה.** `ClosurePanel` נפתח אך ורק מתפריט ההקשר (קליק ימני) של `CalendarGrid`,
והגריד הזה הוא `hidden md:flex`. כלומר בטלפון **לא הייתה שום דרך לחסום חדר** — לא
פגם פריסה, אלא פונקציה חסרה.

**מה נעשה.** שני טריגרים מפורשים ומתויגים "חסימת חדר", שניהם קוראים לאותו
`setPanel({ kind: "closure", … })` ופותחים את אותו `ClosurePanel` היחיד:

| טריגר | מתי נראה | למה |
|---|---|---|
| `.cb-m-close` | מתחת ל-`md`, בכותרת לוח המובייל | נקודת הכניסה של הטלפון בפורטרייט |
| `.cb-touch-close` | ‏`display:none` לעכבר; מופיע רק תחת `(pointer: coarse)` | **טלפון בלנדסקייפ הוא 844×390 — רחב מ-`md`**, ולכן מקבל את עץ הדסקטופ, שבו החסימה נגישה רק בקליק ימני. בלי זה הפער היה נסגר בפורטרייט ונשאר פתוח בלנדסקייפ |

הדסקטופ עם עכבר לא זז בפיקסל: `.cb-touch-close` מוסתר בברירת מחדל ומתגלה לפי
**סוג המצביע**, לא לפי רוחב.

**אין לוגיקה מקבילה.** אין `createClosureAction` מחוץ ל-`ClosurePanel`, אין מופע
שני של הפאנל, ואין state נפרד למובייל — `check:responsive` #14 מקבע את שלושת
הדברים האלה וגם את ה-gating על `can.close`. ה-prefill זהה בצורתו לזה שהתפריט
שולח; החדר נבחר ב-`<select>` שכבר קיים בפאנל, כי לכפתור בכותרת אין שורה שממנה
לגזור חדר.

**מה נמדד חי מול staging** (`closure-mobile.mjs`, 4 viewports):

| viewport | טריגר | פאנל | ‏doc/client | פעולה ראשית בהישג יד | ‏Escape סוגר | הלוח מאחור שרד |
|---|---|---|---|---|---|---|
| ‏390×844 | ‏117×44 בתוך המסך | ‏390×844 | ‏390/390 | ✓ | ✓ | ✓ |
| ‏320×568 | ‏117×44 | ‏320×568 | ‏320/320 | ✓ | ✓ | ✓ |
| ‏740×360 landscape | ‏117×44 | ‏740×360, גוף נגלל | ‏740/740 | ✓ | ✓ | ✓ |
| ‏844×390 landscape | ‏117×44 (הטריגר של המגע) | ‏717×390, גוף נגלל | ‏844/844 | ✓ | ✓ | ✓ |

בכל ארבעתם: `<select>` עם 15 חדרים, כל פקדי הטופס ב-17px (‏אין זום ב-iOS),
‏0 גלילה אופקית, הפוקוס בתוך הפאנל.

**נתיב היצירה נבדק מקצה לקצה**, לא רק הפריסה: דרך הטריגר במובייל נוצרה חסימה
אמיתית (`החדר ייסגר ל־2 לילות` → `החדר נסגר לטווח שנבחר`), היא נמצאה ב-
`guesthub.room_closures` ב-staging, ונמחקה אחרי הבדיקה. הרצה שנייה על אותו טווח
החזירה שגיאה והשאירה את הפאנל פתוח — כלומר גם מסלול הכישלון מתנהג נכון.

**מה עדיין דסקטופ בלבד.** *הסרת* חסימה קיימת (`הסר חסימה` בתפריט ההקשר) לא נוספה
למובייל. היא אינה חלק מ-`ClosurePanel` — זו פעולה נפרדת עם אישור — והוספתה הייתה
לוגיקה חדשה מחוץ להיקף. נרשם כאן במפורש ולא הוסתר.

---

## 12. הפקודות שהורצו

```bash
# שומרים
node scripts/check-responsive.mjs                        # 14/14
node scripts/check-design-system.mjs                     # 6 הפרות, כולן ב-MyTasksScreen (קפוא, קדם-ענף)
STAGING_DATABASE_URL=… node scripts/check-db-isolation.mjs
CHECK_DB_URL=<prod> node scripts/check-db-isolation.mjs  # קריאה בלבד
pnpm typecheck                                           # 0 שגיאות
pnpm lint                                                # 0 שגיאות, 34 אזהרות (קדם-ענף)
pnpm suite                                               # ב-mobile-baseline ואז בענף

# מדידות דפדפן — staging בלבד (:3017 מה-worktree, DB :5434, GoTrue :9989 מאחורי shim :9991)
AUDIT_RESERVATION_ID=$(cat rid.txt) node run-audit.mjs   # המטריצה, 391 תאים
node touch-matrix.mjs                                    # הקשר hasTouch, 66 תאים
node hittest.mjs                                         # elementFromPoint — ground truth
PORTAL_VP=320x568|390x844|844x390 node portals.mjs       # 21 משטחים × 3 viewports
node tables.mjs                                          # 9 הטבלאות ב-320/390
PROBE_DAY=NN node closure-mobile.mjs                     # §11 — כולל יצירה אמיתית
```

**כתיבות ל-staging במהלך האימות:** שלוש רשומות `guesthub.room_closures` עם
`reason='mobile-audit probe'`, שנוצרו דרך ה-UI כדי להוכיח שנתיב היצירה עובד
מקצה לקצה. **כולן נמחקו** (`select count(*) … = 0`). שום כתיבה אחרת, ושום כתיבה
לפרודקשן.

---

## 13. מה נדרש מרונן

1. **‏O-1** — אישור ל-`sudo apt-get install libwayland-server0 libxml2` כדי לאפשר מעבר WebKit.
   **לא בוצע ולא יבוצע בלי מילה מפורשת** — זה מארח משותף שמריץ פרודקשן.
2. **‏O-2** — רשומת מנעול ב-staging כדי לסגור את P-20. לא יוצרו נתוני מנעולים
   מלאכותיים רק כדי שהבודק יעבור.
3. **סקירה אנושית** של תשעת מסכי הכרטיסים החדשים ושל נקודת הכניסה לחסימת חדר.
   הבודק מודד גיאומטריה, לא איכות (‏O-7).
4. **אישור מיזוג.** לא מוזג, לא נפרס, לא נגעתי בפרודקשן — פרט לקריאות
   `SELECT`/`has_*_privilege` בלבד לצורך אימות ההרשאה ב-§8/O-4.

**‏O-3 ו-O-4 נסגרו בסבב הזה** ואינם דורשים דבר.


---

## 14. סבב 4 — פאנל עריכת ההזמנה (§E של הפרומפט)

הבסיס לסבב הזה אינו מדידה אלא **צילומי מכשיר אמיתי** של הבעלים: גוף ההזמנה
מוסט הצידה ומחוץ למסך, ופס גלילה דק לצד הטופס. שניהם שוחזרו במדידה לפני שנגעתי
בקוד, ולכל אחד נמצא שורש נפרד.

### 14.1 · ההיסט האופקי — `align-items` שהחליף ציר

```text
390x844 · לפני
  aside[role=dialog]   390px @ left 0        ← הפאנל עצמו היה תקין
  .dw-bd               scrollWidth 469 > clientWidth 390
  .bw-col-main         443px @ left -79      ← הגוף מחוץ למסך
  183 אלמנטים חורגים מה-viewport
```

`.bw-main` הוא `display:flex` עם `align-items: flex-start`. זה **נכון** לפריסת
השורה: הוא מונע מהעמודה הראשית ומעמודת הסיכום להימתח לגובה זהה. השאילתה
‏`@media (max-width: 1024px)` הופכת אותו ל-`flex-direction: column` — ומאותו רגע
אותה הצהרה בדיוק שולטת על הציר ה**אופקי**, כלומר כל עמודה נמדדת לפי
‏`max-content` במקום להימתח למכל. `.bw-col-main` יצא 443px בתוך קופסה של 338.

התיקון הוא `align-items: stretch` בתוך אותה שאילתה. אין כאן דריסת רוחב ואין
חיתוך: `stretch` הוא מה ש-`column` צריך.

### 14.2 · הפעולה ההרסנית ישבה מחוץ למסך

```text
390x844 · .dw-ft  (‏flex row-reverse, ללא גלישה)
  שמור שינויים   left  24 → right 171   ✓
  סגור           left 181 → right 252   ✓
  בטל הזמנה      left 272 → right 415   ✗  מחוץ ל-390
```

הפוטר הוא אח של הגוף הנגלל, לא חלק ממנו — כלומר שום גלילה לא הייתה מחזירה את
"בטל הזמנה" למסך. `flex-wrap: wrap` + `row-gap` מחזירים אותה לשורה שנייה בלי
לכווץ אף פעולה ובלי לשנות את סדר ה-DOM.

### 14.3 · תפריט הפעולות נפתח לכיוון היציאה מהמסך

`.bk-tb-menu` הוא `inset-inline-start: 0` על טריגר שיושב באשכול ה-inline-end של
הכותרת — כלומר צמוד לקצה השמאלי הפיזי ב-RTL. שם `inset-inline-start` הוא
‏`right: 0`, והתפריט גדל **שמאלה**: נמדד `left:-90` ב-320 ו-`left:-22` ב-390.
תקרת הרוחב שנוספה בסבב הקודם נחוצה אך לא מספיקה — קופסה של 200px ב-left:-90
עדיין בחוץ. עיגון הקצה הנגדי מפנה אותה פנימה. תופעת לוואי שנמדדה: כשהתפריט
הרחיב את קופסת הגלילה של הכותרת, היסט ה-RTL גרר את `.dw-hd` עצמה הצידה
(‏68 → 388 ב-320) — כלומר "הכותרת זזה" היה אותו פגם, לא פגם שני.

### 14.4 · פס הגלילה הדק שנראה בצילום

‏`.thin-scroll` מצהיר `scrollbar-width: thin`. במכשיר מגע ההצהרה הזו **מוותרת**
על מחוון הגלילה הצף של הפלטפורמה ומייצרת פס קלאסי קבוע — בדיוק הפס שנראה
בצילום, והוא גם גוזל רוחב אמיתי מהשדות שלידו. תחת `pointer: coarse` הוא מוחלף
במחוון הפלטפורמה. **זה לא מסיר מכל גלילה:** בגוף הפאנל יש בדיוק סקרולר אנכי
אחד, נמדד ב-320×568, 390×844, 667×375 ו-844×390 — 0 סקרולרים מקוננים.

### 14.5 · מה נמדד אחרי

```text
3 viewports (‏320×568 · 390×844 · 844×390L) × 18 מצבים = 54
  עשרת מקטעי הפאנל          פעולות B.com · פרטי אורח · שהות וחדרים · תמחור
                             ותשלום · מסמכים · הערות · סיכום · פעולות מהירות ·
                             יומן פעילות
  שדות הכרטיס               אמצעי תשלום = כרטיס אשראי (‏D108)
  שני מצבי הרחבה
  חמישה משטחי פעולה מהכותרת מייל · הודעה · אישור הזמנה · ביטול · תפריט פעולות

  54 עברו · 0 נכשלו · 0 לא-נמדדו
  wide=0 · offscreen-focusable=0 · field-over-container=0 · doc-overflow=0
  scrollers=1 בכל אחד מ-54 המצבים
```

**הבודק עצמו תוקן תוך כדי.** בגרסה הראשונה, מצב שבו הפאנל נסגר (הורדת PDF)
החזיר `undefined` ונספר כ"עבר". זו בדיוק צורת הכשל ש-§R מזהיר מפניה, והיא
נמצאה כאן בכלי שלי: עכשיו מצב שלא נמדד מסומן `NOT MEASURED` והפאנל נפתח מחדש
לפני המצב הבא. 9 "מעברים" מדומים הפכו ל-9 מדידות אמיתיות.

---

## 15. סבב 5 — סנכרון מול `main` עדכני, ואימות חוזר מלא

הענף נוצר מעל `main` @ `89d9d6e`. עד שהעבודה הבשילה, `main` התקדם, ולכן **כל
מדידה שנעשתה לפני הסנכרון תיארה עץ שכבר אינו קיים**. הסבב הזה מסנכרן ומודד מחדש.

### 15.1 · מה בדיוק נכנס מ-`main`

```text
89d9d6e  (בסיס הענף)
   └─► 946872d  origin/main בזמן הסנכרון
        09de5a5  fix(booking): holder-only card draft no longer blocks reservation creation
        946872d  Merge pull request #197
```

**שלושה קבצים בלבד** נכנסו: `src/components/reservations/CardFields.tsx` (‏6 שורות),
`scripts/check-card-draft-holder-only-is-empty.mjs` (חדש), ו-`package.json` (שורת
`check:*` אחת).

### 15.2 · שיטת האינטגרציה: מיזוג, לא rebase

`git merge origin/main` לתוך הענף. **‏rebase נשלל במכוון:** הוא היה כותב מחדש 21
קומיטים שכל אחד מהם נושא את המדידה שהצדיקה אותו, ומאבד את הקשר בין תיקון לבין
ההוכחה שלו. מיזוג משאיר את 21 הקומיטים בייט-בבייט.

**התנגשות אחת אפשרית, ולא התממשה.** הקובץ היחיד ששני הצדדים נגעו בו הוא
`package.json`: `main` הוסיף `check:card-draft-holder-only-is-empty` בשורה 61,
הענף הוסיף `check:responsive` בשורה 94. שני מקומות שונים בבלוק הסקריפטים — git
מיזג את שניהם, ואומת ידנית ששניהם קיימים ושה-JSON תקין. `CardFields.tsx` אומת
כזהה-בייט לגרסת `origin/main`, ו-54 קבצי הענף אומתו כזהים לגרסתם שלפני המיזוג.

### 15.3 · מה הורץ מחדש אחרי הסנכרון

| מדידה | תוצאה אחרי הסנכרון | מול סבב 4 |
|---|---|---|
| מטריצה ראשית (‏23 × 17) | ‏391/391 · **0 P0** · 0 גלישה אופקית (doc ו-main) · 0 שגיאות ניווט/JS/probe | זהה |
| הוכחת רינדור | **391/391** · 0 error-boundary · 0 גובה-אפס | זהה |
| מתוכם landscape | ‏92 תאים · **0 P0** | זהה |
| מטריצת מגע (‏22 × 3) | ‏66 תאים · **0 P0** · **0 INPUT-ZOOM** · 166 TAP-SMALL (גודל מצויר) · 0 שגיאות ניווט | זהה תו-בתו |
| בדיקת פגיעה (‏12 מסלולים) | **0 מתחת ל-44×44** · **0 חפיפות** | זהה |
| משטחים צפים ב-390×844 | ‏20/21 נפתחו · 0 רחבים/גבוהים · **0 כשלי Escape** · 0 פוטר מחוץ למסך | זהה |
| חסימת חדר במובייל (‏4 viewports + יצירה חיה) | **ALL CHECKS PASSED** — כולל שמירה בפועל | זהה |
| לוח התעריפים בגובה נמוך | ‏320×568 → **8 שורות** · ‏568×320 ו-844×390 → **6** · ‏390×844 → 12 | זהה |
| מסלול ההדפסה (‏R-24, מזהה אמיתי) | ‏17 תאים · **0 P0 · 0 P1** | נמדד עכשיו עם `AUDIT_RESERVATION_ID` |
| `check:responsive` | **18/18** | זהה |
| ‏suite מלא | ‏86/8/1 מתוך 95, מול 85/8/1 מתוך 94 ב-main נקי — **0 רגרסיות** | הבייסליין הוחלף ל-`946872d` |
| `typecheck` · `lint` | ‏0 שגיאות · 34 אזהרות — **זהה בשני העצים** | נמדד עכשיו על שניהם |
| `pnpm build` | **‏exit 0 בשני העצים** | ראה §15.5 |

### 15.4 · פאנל עריכת ההזמנה — ‏4 viewports, לא 3

הפרומפט דרש במפורש גם `568×320`. הכיסוי הורחב מ-3 viewports ל-4, כלומר
**‏72 מצבים** במקום 54:

```text
320×568 · 390×844 · 568×320L · 844×390L   ×   18 מצבים   =   72
  72 עברו · 0 נכשלו · 0 לא-נמדדו
  wide=0 · offscreen-focusable=0 · field-over-container=0 · doc-overflow=0
  scrollers=1 בכל אחד מ-72 המצבים
```

הפוטר נמדד בנפרד, כי "הפעולה ההרסנית על המסך" היא טענה שצריך לראות במספרים:

| viewport | "שמור שינויים" | "סגור" | **"בטל הזמנה"** | תחתית הפוטר מול גובה המסך |
|---|---|---|---|---|
| ‏320×568 | ‏147×44 · x 24→171 | ‏71×44 · x 181→252 | ‏143×44 · x **24→167** ✓ | ‏568 / 568 |
| ‏390×844 | ‏147×44 · x 24→171 | ‏71×44 · x 181→252 | ‏143×44 · x **24→167** ✓ | ‏844 / 844 |
| ‏568×320L | ‏147×44 · x 24→171 | ‏71×44 · x 181→252 | ‏143×44 · x **401→544** ✓ | ‏320 / 320 |
| ‏844×390L | ‏147×44 · x 24→171 | ‏71×44 · x 181→252 | ‏143×44 · x **677→820** ✓ | ‏390 / 390 |

בפורטרייט הפוטר נעטף לשתי שורות (‏§14.2) ולכן "בטל הזמנה" יורדת שורה; בלנדסקייפ
שלושתן נכנסות לשורה אחת. בכל ארבעתם כל הכפתורים בגובה **44px** ובתוך המסך.

**שדות הכרטיס — טענה שנבדקה, לא הונחה.** מצב "אמצעי תשלום = כרטיס אשראי" עובר את
בדיקות הגיאומטריה — אבל **מצב שלא מרנדר כלום עובר אותן גם כן**. לכן נמדד בנפרד:

```text
4 שדות כרטיס (בעל הכרטיס · מספר · תוקף · CVV) בכל אחד מ-4 ה-viewports
  כולם באותו section אחד (כרטיס "תמחור ותשלום") — לא במשטח חדש
  dialogs on page = 1  (הפאנל עצמו; אין portal שני)
  כל שדה 44px גובה, בתוך המסך אופקית, ולא רחב ממכל האב
```

שלושת ה-`input` שכן יושבים מחוץ לפאנל זוהו אחד-אחד ואינם דליפה: שדה
`$ACTION_ID` של Server Action, חיפוש ה-TopBar, וחיפוש מסך ההזמנות.

### 15.5 · ‏O-8 לא משוחזר — הסיבה האמיתית הייתה התנגשות `.next`

סבב 4 רשם את בניית ה-worktree כמלכודת ידועה ("‏CSS חסר"). בסבב הזה **שני** העצים
נבנו נקי:

```text
main נקי @ 946872d : BUILD_EXIT=0 · chunks/c1810452bbd94d17.css  41.4 kB
הענף אחרי המיזוג  : BUILD_EXIT=0 · chunks/a39bd4ddbcd259ab.css  42.9 kB
```

הסיבה לתסמין הקודם אותרה: `next dev` רץ באותו worktree וכותב לאותו `.next`.
בנייה תוך כדי מוחקת מתחת לרגליו את הצ'אנקים, ואז **שרת ה-dev מתחיל להגיש 500**
עם `ENOENT … _buildManifest.js.tmp`. זה מה שקרה, וזה גם מה שהחזיר את המסך למצב
שגיאה במדידות. הפתרון: לעצור את שרת ה-dev לפני `pnpm build`, ולא להסיק מכך דבר
על פרודקשן.

**השכבה הרספונסיבית מאומתת בתוך ה-CSS הבנוי**, לא רק ב-dev: `pointer:coarse`,
‏`dvh`, `align-items:stretch` של `.bw-main`, `.bk-tb-menu` ו-`scrollbar-width:none`
כולם נמצאים בצ'אנק המיוצר. ב-CSS של `main` אין ולו כלל `pointer:coarse` אחד.

### 15.6 · מלכודת חדשה שנמצאה בסביבת המדידה

**‏`pnpm suite` ו-מדידת דפדפן לא יכולות לרוץ יחד באותו worktree.** ‏`run-checks.mjs`
כותב מחדש את `.env.local` לכל שומר (עם גיבוי `.env.local.suite-backup-<RUN_ID>`),
ו-`next dev` טוען אותו מחדש — כלומר מפתחות Supabase נעלמים באמצע, ה-middleware
זורק, ו-`/login` מחזיר 500. התסמין בצד הבודק הוא
`page.fill: Timeout … waiting for locator('#identifier')`, שקל לפרש בטעות כ"עומס".

זו לא תקלה בשומר — הבידוד הוא בכוונה — אבל היא **חייבת** להירשם: הרצה מקבילה
מייצרת כשל שנראה כמו כשל אפליקציה ואינו. בסבב הזה שתי הרצות בודק נפסלו מהסיבה
הזו והורצו מחדש על מכונה שקטה.

### 15.7 · מה לא השתנה

`WebKit` (‏O-1) עדיין חסום — דורש `sudo` על מארח משותף שמריץ פרודקשן; לא הותקן דבר.
`P-20` (‏drawer הקודים) עדיין חסום — ‏0 שורות `ttlock_locks` ב-staging; לא הומצא מידע.
`P-45` ו-`/housekeeping/my-tasks` נשארו **HUMAN REVIEW** כפי שהיו.
