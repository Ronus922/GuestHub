# דוח התאמת מובייל ורספונסיביות — GuestHub

- **תאריך:** 2026-08-13 (סבב 2)
- **ענף:** `fix/mobile-audit-2026-08-12` (בסיס `main` @ `89d9d6e`) · worktree `~/worktrees/mobile-audit`
- **בייסליין להשוואה:** `~/worktrees/mobile-baseline` — `main` נקי
- **סביבת אימות:** האפליקציה מה-worktree על `127.0.0.1:3017` מול `guesthub-staging-db`
  (:5434, סכימה 083) + `guesthub-staging-auth` (:9989), משתמש `admin@ginot.co.il`.
  **אפס נגיעה בפרודקשן** — לא ב-DB, לא בפורט 3007, לא ב-pm2, ללא מיזוג וללא deploy.

> **על ניסוח:** אין כאן טענה ש"האתר רספונסיבי ב-100%". יש מדידות: מה נמדד, באיזה
> מנוע, ומה נשאר פתוח. כל טענה על iOS מסומנת כאימות **מנוע/מפרט** ולא כאימות על
> Safari אמיתי — ראה §9.

---

## 1. תקציר

| מדד | לפני | סבב 1 | סבב 2 |
|---|---|---|---|
| תאים שנמדדו (route × viewport) | 286 | 286 | **391** (23 × 17) |
| ‏viewports | 13 | 13 | **17** (+4 landscape של טלפון) |
| ממצאי **P0** (מטריצת pointer דק) | 82 | 0 | **0** מתוך 391 |
| ממצאי **P0** (מטריצת מגע) | — | — | **0** מתוך 66 |
| גלילה אופקית ברמת המסמך או אזור התוכן | יש | 0 | **0** |
| טבלאות רחבות עם ייצוג כרטיסים במובייל | 0/9 | 2/9 | **9/9** |
| פקדים מתחת ל-44×44 במגע | 833 | 833 | **0** |
| אזורי מגע חופפים | — | — | **0** |
| משטחים צפים שנפתחו ונמדדו | — | 13/19 | **19/20** |
| משטחים שלא נסגרו ב-Escape | — | 3 | **0** |
| ‏`check:responsive` | לא היה | 9 בדיקות | **13 בדיקות** |

---

## 2. תשע הטבלאות — ייצוג כרטיסים ייעודי (9/9)

**העיקרון:** לא לשכפל JSX ולא ליצור נתיב לוגי שני. ארבע מהטבלאות הן CSS-grid, וכל
שורה נושאת התנהגות חיה — גרירה לשינוי סדר, מתגי הפעלה, כוכב ברירת מחדל עם שבע
סיבות השבתה נפרדות, אישור מחיקה דו-שלבי. לכן **השורה הופכת לכרטיס ב-CSS**, באותו
DOM: שינוי ה-markup הוא `data-label` אחד לתא, בלי state ובלי לוגיקה.

| # | מסך | שיטה | הפעולות שנשמרו |
|---|---|---|---|
| 1 | `/reservations` | `MobileRecordCard` | פתיחת פאנל, סטטוסים, יתרה |
| 2 | `/staff` | `MobileRecordCard` | עריכת עובד |
| 3 | `/guests` | `MobileRecordCard` | פתיחת פרופיל |
| 4 | `/permissions` | כרטיס לכל הרשאה + רשימת תפקידים | אותו `<Cell>`, אותו `toggle`, אותה מפת `pending` |
| 5 | סטטוסי הזמנה | `mcard` | סדר ▲▼, מתג, כוכב, עריכה, מחיקה דו-שלבית |
| 6 | אמצעי תשלום | `mcard` | ידית גרירה, מתג, מחיקה דו-שלבית |
| 7 | `/locks` | `mcard` | בחירה, שיוך, קודים, חשיפה, העתקה |
| 8 | מיפויי Beds24 | `mcard-table` | עריכה, שמירה, ביטול מיפוי |
| 9 | רשימת התבניות + הארכיון | `mcard` | תצוגה מקדימה, שכפול, אוטומציה, ארכיון, שחזור |

בנוסף הומרה גם טבלת שגיאות הסנכרון ב-`/channels` (‏`min-w-[560px]`) — לא הייתה
ברשימה, אבל גללה אופקית באותה מידה.

### שתי מלכודות שנמדדו ולא נוחשו

**‏1. סדר שכבות ה-CSS.** `status-settings.css`, `payment-methods.css` ו-`locks.css`
מצהירים **מחוץ** ל-`@layer components`. בקסקייד, כלל לא-מרובד מנצח כל כלל מרובד
ללא קשר לספציפיות — ולכן `.mcard-rows > .mcard-row` (0,2,0) הפסיד ל-
`.ws-tbl { min-width: 830px }` (0,1,0). נמדד: השורה נשארה רשת של שבעה מסלולים
והמקטע עדיין גלל 830px בתוך כרטיס 336px. הבלוק הועבר להיות לא-מרובד — וזה גם מה
שמאפשר לו לדרוס `overflow-x-auto` של Tailwind, כי utilities הן שכבה גם הן.
‏`check:responsive` #13 נועל את סדר השכבות הזה.

**‏2. סגנון inline.** רשימת הארכיון הצהירה `minWidth: 760` ב-`style`. סגנון inline
מנצח כל גיליון סגנון, ולכן הרצפה הזו הייתה שורדת את פריסת הכרטיסים. חולץ ל-`.gc-arch`.

---

## 3. אזורי מגע — 44×44 בלי להגדיל את הממשק

בייסליין מדוד: **833 פקדים מתחת ל-44×44**, רובם הגדלים הקנוניים של §4 —
`.icon-btn` 36×36 (‏247 מופעים), `.btn-sm` בגובה 36, `.chip` 28, `.cb-nav` 36×36.
הגדלים האלה **הם** מערכת העיצוב, ו-`check:design` אוכף אותם כקבוצה סגורה.

**הפתרון:** כל פקד שומר על גודלו המצויר ומקרין `::after` שקוף וממורכז עד 44×44,
רק תחת `pointer: coarse`. הכלל מגביל את עצמו — `width/height: 100%` עם רצפת 44px
פירושו שפקד שכבר ≥44 מקבל `::after` בגודלו בדיוק: אפס הרחבה, אפס חפיפה.
גנרי דרך `:where()` (ספציפיות 0) ולא רשימת מחלקות, כי רשימה מנויה **פספסה** 325
תיבות סימון בהרשאות, כל Switch, כל כפתורי הסגמנט ואת שדות החיפוש.

**‏`:disabled` ו-`[aria-disabled="true"]` לא מקבלים אזור מגע כלל** — פקד מושבת נשאר מת.

**חפיפות טופלו במדידה, לא בהנחה.** איפה ששני פקדים קטנים היו צמודים מדי, ה-gap
הורחב למינימום שמבטל את החפיפה (‏44 = 36+8, ‏44 = 28+16). שני חצי הסדר הזעירים
(‏22×16 במרחק 2px) קיבלו קופסה **אמיתית** של 44 — הרחבה מרכזית לא יכולה לעזור שם,
וקופסה אמיתית נלקחת בחשבון בפריסה ולכן חפיפה בלתי אפשרית מבנית. האייקונים לא שונו.

### הבאג שהיה בתיקון עצמו

הגרסה הראשונה עיגנה את ה-`::after` ב-`inset-inline-start: 50%` עם
`translate: -50% -50%`. במסמך RTL זה נפתר ל-`right: 50%`, ו-`translate` הוא פיזי
ו**לא** מתהפך איתו — כך שאזור המגע נדחף ברוחב מלא **שמאלה** מהפקד במקום למרכזו.

זה לא נתפס בסבב הראשון כי המדידה **חישבה** איפה ה-`::after` אמור להיות והסיקה
"‏0 מתחת ל-44" — מדידה שמאשרת את עצמה. בדיקת פגיעה אמיתית
(`document.elementFromPoint`) על נקודה 21px מעל מרכז כפתור אייקון 36px החזירה
אלמנט אחר לגמרי: המטרה של 44px לא הייתה קיימת במקום שבו אגודל נוחת.

התיקון הוא מרכוז ב-`left`/`top` **פיזיים** — transform לא יכול לשקף, ולכן גם העוגן
שלו לא צריך. `check:responsive` #10 אוסר עכשיו במפורש את הצורה השבורה.

**נמדד אחרי התיקון (ראה §10.3): 0 מטרות מתות, 0 חפיפות.**

---

## 4. משטחים צפים — 19 מתוך 20 נפתחו ונמדדו

| מזהה | משטח | ‏W×H | רוחב | גובה | סגירה | נעילה | ‏Esc | ‏P0 |
|---|---|---|---|---|---|---|---|---|
| P-01 | BookingPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-02 | EditReservationPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-03 | SimulatorPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-04 | RatePlanWizard | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-05 | RoomWizard | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-06 | GroupUpdatePanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-07 | AreaPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-08 | TemplateEditor | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-14 | פאנל אורח | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-15 | ClosurePanel | 768×800¹ | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-16 | אמצעי תשלום | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-17 | מדיניות ביטול | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-21 | SourcesDrawer | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-22 | EmployeeSidePanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-23 | CellDetailPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-24 | OverridesPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-35 | ‏bottom sheet | 390×293 | ✔ | ✔ | ✔ | — | ✔ | 0 |
| P-38 | StatusPopover | 316×354 | ✔ | ✔ | ✔ | — | ✔ | 0 |
| P-45 | מגירת ניווט מובייל | 250×844 | ✔ | ✔ | ✖² | — | ✔ | 0 |
| P-20 | ‏drawer הקודים ב-`/locks` | — | — | — | — | — | — | **OPEN** |

¹ ל-ClosurePanel **אין נקודת כניסה במובייל כלל** — הוא נפתח רק מתפריט ההקשר של לוח
הדסקטופ (`.cb-rcell` → "סגור חדר"), והלוח הזה הוא `hidden md:flex`. נמדד היכן שהוא
חי בפועל. **זה ממצא, לא דילוג** — ראה O-3.
² נסגרת בסקרין וב-Escape; אין בה כפתור ✕ ייעודי.

**‏0 משטחים רחבים מה-viewport · 0 גבוהים ממנו · 0 שלא נסגרו ב-Escape · 0 footer מחוץ למסך.**

### Escape

שני משטחים לא ענו ל-Escape כלל: הגיליון התחתון ו-popover הסטטוס בחדרים. שניהם
מאזינים עכשיו **בשלב ה-capture** ועוצרים את האירוע — הגיליון יכול לשבת מעל הלוח,
ובלי עצירה Escape אחד היה סוגר את שניהם. הגיליון מעביר פוקוס פנימה בפתיחה ומחזיר
אותו לטריגר בסגירה. גלילה, נגיעה וסקרין לא שונו. ‏`check:responsive` #12 נועל.

**מלכודת מדידה שתוקנה:** ה-SidePanel יוצא דרך `AnimatePresence` עד 1.2 שניות, והמתנה
של 700ms קראה פאנל באמצע יציאה כ"לא נסגר". שלושת ה"כשלונות" בסבב 1 היו ארטיפקט של
הבודק, לא פגם.

---

## 5. ‏`/reservations/[id]/print`

נבדק עם **מזהה הזמנה אמיתי מ-staging**, שנקרא מה-DB בקריאה בלבד
(`select id from guesthub.reservations order by created_at desc limit 1`) — לא הומצא.
נמדד ב-390×844, ‏768×1024, ‏1280×800 ו-844×390 (landscape): טעינה תקינה בכל ארבעה,
**0 P0, 0 P1**, ללא גלילה אופקית.

---

## 6. ‏Landscape של טלפון

נוספו למטריצה ארבעה viewports שבהם **הגובה** הוא האילוץ ולא הרוחב:
`568×320 · 667×375 · 844×390 · 932×430`. גובה של 375px צריך להכיל סרגל עליון sticky
בן 64px, footer של מגירה, ועם מקלדת וירטואלית כמחצית מזה.

הכיסוי חל על כל 23 המסלולים — הניווט, מסכי ההזמנה, הלוח, הטפסים והכרטיסים החדשים.
המשטחים הצפים נמדדו ב-390×844 בלבד (ראה O-5).

---

## 7. תיקונים עיקריים בסבב הזה

| # | תיקון |
|---|---|
| 1 | ‏7 טבלאות → כרטיסי מובייל (+ טבלת שגיאות הסנכרון) |
| 2 | בלוק row→card הועבר מחוץ ל-`@layer components` — אחרת אינו פועל כלל |
| 3 | ‏`minWidth: 760` inline בארכיון חולץ ל-CSS |
| 4 | אזורי מגע 44×44 גנריים ב-`pointer: coarse`, שקופים, לא על פקד מושבת |
| 5 | הרחבת gap היכן שאזורי המגע חפפו; ‏`.ws-ord` קיבל קופסה אמיתית |
| 6 | ‏`Escape` לגיליון התחתון ול-StatusPopover — capture + החזרת פוקוס |
| 7 | ‏`.dw-hd` נעטף — כותרת חלון ההזמנה נמדדה 428px בתוך מגירה של 390px |
| 8 | ‏`.card-hd` נעטף — ב-320px כותרת המקטע נמעכה ל-24px ונחתכה |
| 9 | ‏`check:responsive` 9 → 13 בדיקות |

---

## 8. ‏OPEN ISSUE

### O-1 · ‏WebKit לא רץ — חסרות תלויות מערכת

‏`webkit-2311` הותקן בהצלחה, אבל ההרצה נחסמת:

```
Host system is missing dependencies to run browsers.
  sudo apt-get install libwayland-server0 libxml2
```

זהו שינוי ברמת המערכת על מארח משותף שמריץ פרודקשן, ודורש `sudo`. **לא בוצע** —
נדרש אישור מפורש. עד אז כל הבדיקות רצו ב-Chromium בלבד.

### O-2 · ‏P-20 — ‏drawer הקודים ב-`/locks` לא נפתח

חסם נתונים מדויק: `select count(*) from guesthub.ttlock_locks` על staging מחזיר **0**.
ה-drawer נפתח רק אחרי בחירת שורת מנעול, ואין שורות. לא פגם — היעדר נתונים. הפתרון:
להזין רשומת מנעול ל-staging, או להריץ מול סביבה שיש בה מנעולים.

### O-3 · ל-ClosurePanel אין נקודת כניסה במובייל

נפתח רק מתפריט ההקשר (קליק ימני) של לוח הדסקטופ, והלוח מוסתר מתחת ל-`md`. כלומר
**במובייל אין דרך ליצור חסימת חדר**. הפאנל עצמו נמדד ותקין ב-768px. זו החלטת מוצר,
לא באג פריסה.

### O-4 · ‏`/staff` נמדד קודם כדף שגיאה

ב-staging לא הייתה ל-`guesthub_app` הרשאת `SELECT` על סכימת `auth`, ולכן `/staff`
החזיר error boundary. **כל מדידה של המסך הזה לפני התיקון מדדה דף שגיאה.** ההרשאה
ניתנה ב-staging והמסך אומת מחדש (7 כרטיסים, 7 שורות טבלה). **יש לוודא שההרשאה
קיימת בפרודקשן** — אם לא, `/staff` שבור גם שם.

### O-5 · המשטחים הצפים נמדדו ב-390×844 בלבד

לא ב-320px ולא ב-landscape. הפריסה שלהם viewport-relative ולכן הסיכון נמוך, אבל
זו הסקה ולא מדידה.

### O-6 · צפיפות הנתונים ב-staging נמוכה מפרודקשן

‏83 הזמנות, 15 חדרים, 7 משתמשים, 0 מנעולים. **המדידות תקפות למבנה, לא לנפח.**

### O-7 · הבודק מודד גיאומטריה, לא איכות

הוא לא קובע אם הכרטיס שנבנה הוא מה שפקיד קבלה באמת רוצה לראות בטלפון. אין תחליף
לסקירה אנושית של המסכים החדשים.

---

## 9. טענות iOS — סיווג מדויק

| טענה | סטטוס האימות |
|---|---|
| ‏`100dvh` פותר את חיתוך סרגל הכתובת | **מפרט + Chromium.** לא נבדק על Safari |
| ‏`env(safe-area-inset-*)` נכנס לתוקף עם `viewportFit: cover` | **מפרט.** לא נבדק על מכשיר עם notch |
| ‏17px מונע זום בפוקוס | **מפרט + מדידת computed style** בהקשר `pointer: coarse` ‏(17/17/17). הזום עצמו לא נצפה |
| אזורי מגע 44×44 | **נמדד** גיאומטרית ב-Chromium בהקשר מגע |
| ‏`Escape`, פוקוס, נעילת גלילה | **נמדד** ב-Chromium |

אין בדוח הזה שום טענה שמשהו "אומת על iOS Safari".

---

## 10. אימות סופי

### 10.1 · מטריצה מלאה — ‏pointer דק (עכבר)

‏23 מסלולים × 17 viewports = **391 תאים**.

```text
391/391 תאים נטענו · 0 שגיאות ניווט · 0 שגיאות JS · 0 ממצאי P0
מתוכם 92 תאי landscape של טלפון — 0 P0
```

ההרכב שנותר (הכול P1): 1,139 TAP-SMALL · 250 INPUT-ZOOM · 14 CLIPPED · 9 TRUNCATED.
**שתי הראשונות אינן ממצאים בהקשר הזה** — הן נמדדות ב-pointer דק, שבו כלל ה-44px
וכלל ה-17px **במכוון** אינם חלים. עכבר לא צריך 44px. המדידה הרלוונטית היא 10.2.

### 10.2 · מטריצה בהקשר **מגע** (`hasTouch`)

‏22 מסלולים × 3 viewports (‏390×844, ‏844×390 landscape, ‏320×568) = **66 תאים**.

```text
0 ממצאי P0 · 0 INPUT-ZOOM · 0 שגיאות ניווט · 26 CLIPPED ברמת P1
```

‏`INPUT-ZOOM = 0` הוא האימות שכלל ה-17px באמת נדלק במגע — בסבב הקודם הוא היה שבור
בשקט (‏select ו-textarea הפסידו בספציפיות) ואיש לא היה יודע בלי המדידה הזו.

‏`TAP-SMALL = 166` הוא **הגודל המצויר**, שנשמר בכוונה. אזור המגע האפקטיבי נמדד בנפרד:

### 10.3 · בדיקת פגיעה אמיתית (ground truth)

במקום לחשב איפה אזור המגע אמור להיות, נשאל הדפדפן מה נמצא בנקודה:

```text
14 מסלולים · כל פקד קטן מ-44×44 · 4 נקודות גבול (±21px בשני הצירים)
0 centre-miss · 0 מטרות מתות · 470/486 נקודות גבול פוגעות בפקד
0 אזורי מגע חופפים
```

**כך התגלה באג שהכנסתי בעצמי:** המדידה הקודמת חישבה איפה ה-`::after` אמור להיות
והסיקה "0 מתחת ל-44" — מדידה שמאשרת את עצמה. בדיקת הפגיעה גילתה
`edge(+21px)-hit = 0`: אזור המגע ישב **ליד** הפקד ולא עליו, כי
`inset-inline-start: 50%` נפתר ל-`right: 50%` ב-RTL בעוד ש-`translate` נשאר פיזי.

### 10.4 · משטחים צפים

‏19 מתוך 20 נפתחו בשרשרת סלקטורים דטרמיניסטית. ‏0 רחבים מה-viewport, ‏0 גבוהים ממנו,
‏0 שלא נסגרו ב-Escape, ‏0 footer מחוץ למסך, ‏0 ממצאי P0 בתוך פאנל.

### 10.5 · תשע הטבלאות

נמדד ב-320px וב-390px: `doc == client` ו-`main == client` בכל אחת, ‏**0 גוללים אופקיים**
בתוך תשעת המסכים. שני הגוללים שנשארו בעמודים האלה הם **סרגלי טאבים**
(‏`.rv-tabsbar`, ‏`.gc-tabs`) — לא טבלאות נתונים, וגלילה אופקית בהם היא הדפוס המכוון
והמתועד בפרויקט.

### 10.6 · ‏suite מלא — ענף מול main נקי

| | main (`mobile-baseline`) | הענף |
|---|---|---|
| עברו | 80 | **85** |
| נכשלו | 9 | **8** |
| לא ניתן להרצה | 4 | **1** |
| סה"כ | 93 | **94** |

‏main נכשל ב: `check:design`, `check:effective-state`, `check:guard-roots`,
`check:guest-communications-db`, `check:inventory`, `check:rate-grid`,
`check:room-picker-window`, `check:sellability`, `check:supply-chain`.
הענף נכשל **באותה רשימה פחות `check:room-picker-window`**.

**‏0 רגרסיות.** כל כישלון בענף נכשל גם ב-main. `check:responsive` הוא הבדיקה ה-94.

### 10.7 · ‏git diff

‏50 קבצים, ‏+2,401/−156. נסרק שורה-שורה: אין שינוי שאינו מובייל/RTL. הקובץ עם הכי
הרבה שורות שהשתנו מחוץ ל-CSS הוא `TaskDispatchBoard.tsx`, ושם **כל** השינויים הם
`text-right` → `text-start`.

---

## 13. מה נדרש מרונן

1. **‏O-1** — אישור ל-`sudo apt-get install libwayland-server0 libxml2` כדי לאפשר מעבר WebKit.
2. **‏O-2** — רשומת מנעול ב-staging כדי לסגור את P-20.
3. **‏O-3** — האם חסימת חדר צריכה נקודת כניסה במובייל.
4. **‏O-4** — לוודא ש-`guesthub_app` בפרודקשן קורא את סכימת `auth`.
5. **אישור מיזוג.** אין deploy.
