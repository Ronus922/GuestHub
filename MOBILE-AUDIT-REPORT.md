# דוח התאמת מובייל ורספונסיביות — GuestHub

- **תאריך:** 2026-08-12
- **ענף:** `fix/mobile-audit-2026-08-12` (בסיס `main` @ `89d9d6e`)
- **worktree:** `~/worktrees/mobile-audit` · **בייסליין להשוואה:** `~/worktrees/mobile-baseline` (main נקי)
- **סביבת אימות:** אפליקציה מה-worktree על `127.0.0.1:3017` מול `guesthub-staging-db`
  (:5434, סכימה עודכנה ל-083) + `guesthub-staging-auth` (:9989), משתמש
  `admin@ginot.co.il` (super_admin). **אפס נגיעה בפרודקשן** — לא ב-DB, לא בפורט 3007,
  לא ב-pm2. תואם ל-`scripts/lib/e2e-write-guard.mjs`.

> **הערה על ניסוח:** אין בדוח הזה טענה שהאתר "רספונסיבי ב-100%". מה שיש כאן הוא
> מדידות: מה נמדד, איפה, ומה נשאר פתוח.

---

## 1. תקציר מנהלים

| מדד | לפני | אחרי |
|---|---|---|
| תאים שנמדדו (route × viewport) | 286 | 286 |
| תאים שנטענו בהצלחה | 286 | **286** |
| ממצאי **P0** (שובר שימוש) | **82** | **0** |
| ממצאי **P1** | 1,390 | 1,038 |
| מסלולים עם P0 | 6 מתוך 22 | 0 |
| שגיאות JS בדף | 0 | 0 |
| משטחים צפים שנפתחו ונמדדו בפועל | — | 13 (מתוך 19 שנוסו) |
| משטח צף רחב או גבוה מה-viewport | — | 0 |
| ‏`documentElement.scrollWidth > clientWidth` | 0 | 0 |
| שומר רגרסיה רספונסיבי ב-suite | **0 מתוך ~110 בדיקות** | `check:responsive`, 9 בדיקות |

**8 קומיטים**, כולם ב-worktree, **ללא deploy וללא מיזוג** — ממתינים לאישור.

---

## 2. מה נבדק — כל route

‏22 יעדי URL × 13 רוחבי מסך. אף מסלול לא הושמט.

| ID | Route | לפני P0 | אחרי P0 | סטטוס |
|---|---|---|---|---|
| R-01 | `/dashboard` | 8 | 0 | FIXED + PASS |
| R-02 | `/calendar` | 32 | 0 | FIXED + PASS |
| R-03 | `/rates` | 8 | 0 | FIXED + PASS |
| R-04 | `/rate-plans` | 0 | 0 | PASS |
| R-05 | `/reservations` | 0 | 0 | FIXED + PASS (כרטיסי מובייל) |
| R-06 | `/guests` | 0 | 0 | FIXED + PASS (min-width מותנה הוסר) |
| R-07 | `/rooms` | 0 | 0 | PASS |
| R-08 | `/housekeeping` | 0 | 0 | PASS |
| R-09 | `/maintenance` | 0 | 0 | PASS |
| R-10 | `/locks` | 0 | 0 | PASS |
| R-11 | `/channels` | 0 | 0 | PASS |
| R-12 | `/settings` (9 סקשנים) | 0 | 0 | PASS |
| R-13 | `/staff` | 0 | 0 | FIXED + PASS (כרטיסי מובייל) |
| R-14 | `/permissions` | 0 | 0 | PASS |
| R-15 | `/communications/templates` | 16 | 0 | FIXED + PASS |
| R-16 | `/communications/automations` | 9 | 0 | FIXED + PASS |
| R-17 | `/communications/history` | 0 | 0 | PASS |
| R-18 | `/communications/channels` | 9 | 0 | FIXED + PASS |
| R-19 | `/communications/archive` | 0 | 0 | PASS |
| R-20 | `/login` | 0 | 0 | FIXED + PASS (`min-h-svh`) |
| R-21 | `/housekeeping/my-tasks` | 0 | 0 | FIXED + PASS (`min-h-svh` + safe-area) |
| R-22 | ‏404 כלשהו | — | 0 | FIXED + PASS (`not-found.tsx` נוצר) |
| R-23 | `/` | — | — | NOT APPLICABLE — redirect בלבד |
| R-24 | `/reservations/[id]/print` | — | — | **OPEN** — ראה §8 |

## 3. אינוונטר Stage 2 — ספירה

‏**155 פריטים** ב-10 קטגוריות (`docs/audit/MOBILE_RESPONSIVE_INVENTORY.md`).

| קטגוריה | פריטים | PASS | FIXED + PASS | OPEN | N/A |
|---|---|---|---|---|---|
| ‏R — routes | 24 | 12 | 10 | 1 | 1 |
| ‏L — layouts ומצבי מסך | 11 | 6 | 5 | 0 | 0 |
| ‏P — משטחים צפים ופורטלים | 45 | 20 | 13 | 12 | 0 |
| ‏T — טבלאות ורשתות | 15 | 6 | 3 | 6 | 0 |
| ‏S — סקשני הגדרות | 10 | 9 | 1 | 0 | 0 |
| ‏W — חלונות דשבורד | 13 | 11 | 2 | 0 | 0 |
| ‏F — טפסים ובוררי תאריך | 11 | 7 | 3 | 1 | 0 |
| ‏K — sticky/fixed | 12 | 10 | 2 | 0 | 0 |
| ‏G — יסודות רוחביים | 8 | 1 | 7 | 0 | 0 |
| ‏D — ‏RTL | 6 | 3 | 3 | 0 | 0 |
| **סה"כ** | **155** | **85** | **49** | **20** | **1** |

---

## 4. בעיות רספונסיביות שנמצאו — לפי חומרה

### היו P0 (שברו שימוש בפועל)

1. **‏`Shell` היה `h-screen overflow-hidden`.** ב-iOS זה ה-viewport ה"גדול" — האפליקציה
   גבוהה בסרגל כתובת אחד מהשטח הנראה, ו-`overflow-hidden` אומר ששום גלילה לא
   מחזירה את הרצועה האבודה.
2. **לוח המובייל רינדר הזמנות מחוץ לחלון הנראה.** ה-loader מביא 3 שבועות/חודש, ציר
   הזמן חותך 3/5/7 ימים — אבל הסרגלים רונדרו לכל הסט. `barGeometry` החזיר
   ‏`start: 370%`, והכפתורים נפרסו כמה מאות פיקסלים מחוץ לכרטיס. בלתי נראים רק מפני
   שהכרטיס חותך — אבל פקדים אמיתיים בעלי פוקוס ש-Tab נחת עליהם.
3. **‏`/rates` גלש אופקית ברמת אזור התוכן** מ-390px ועד 768px — אשכול ניווט התאריכים
   הוא 514px ללא `flex-wrap`.
4. **אשכול הפילטרים בכותרת התקשורת נבלע.** `flex: none` + select 230px: 524px של
   פקדים בתוך כרטיס 332px, ו-`overflow: hidden` של הכרטיס בלע את הקצה. **גם ב-768px**
   חסרו 64px — עמודת התוכן היא 460px אחרי ה-shell, ולכן התיקון נשען על המכל ולא על
   ‏breakpoint.
5. **‏`.gl-rowg` הצהיר `min-width: 1240px` בתוך `@media (max-width: 1200px)`** — הרשת
   התרחבה ככל שהמסך הצטמצם.
6. **תוכן שנחתך בתוך פאנלים** (מתגלה רק בפתיחה בפועל): סרגל השלבים של שני האשפים
   (~455px בפאנל 390px), כרטיסי החדרים בעדכון הקבוצתי, ו-3 שורות פקדים
   ב-`CellDetailPanel` — כולן `flex-1` בלי `min-width: 0`.
7. **‏`.arr-cols` בחלון ההגעות** היה `1fr 1fr` קשיח; ב-320px כל עמודה ~130px והשמות
   נחתכו.

### היו P1 מערכתיים

8. **זום ב-iOS בכל שדה באפליקציה.** `.field-input` הוא 15px; ספארי מזגזג את העמוד
   בכל פוקוס על שדה מתחת ל-16px — כלומר על כל input, select ו-textarea, בכל מסך.
9. **‏`.popover` היה 316px שטוח** ללא `max-width`/`max-height`: גלש בכל מסך צר מ-~340px,
   ותפריט ארוך לא ניתן היה לגלול.
10. **‏`.modal` הגביל רוחב אבל לא גובה**, ו-`overflow: hidden` מנע גם גלילה — כפתורי
    הפעולה נדחפו מתחת למסך ללא דרך להגיע אליהם.
11. **‏`.cb-sheet` ללא תקרת גובה** — כרטיס ארוך גדל מעבר לראש המסך, כולל כפתור הסגירה.
12. **מגירת ה-SidePanel הייתה 60% בכל רוחב מ-640px ומעלה** — טאבלט 700px קיבל פאנל
    ‏420px לצד עמוד משותק.
13. **אין `export const viewport`** בשום קובץ → `viewport-fit` לא מוגדר → כל
    ‏`env(safe-area-inset-*)` באפליקציה נפתר ל-0.
14. **אין `not-found.tsx`** — 404 קיבל דף Next באנגלית LTR בתוך `dir="rtl"`.

---

## 5. תיקונים עיקריים

| # | תיקון | קובץ |
|---|---|---|
| F1 | `export const viewport` עם `viewportFit: "cover"`, **ללא** `maximumScale` | `src/app/layout.tsx` |
| F2 | ‏17px לפקדי טופס במגע (`pointer: coarse` **או** `max-width: 767px`) | `styles/responsive.css` |
| F3 | ‏`.app-shell` / `.vh-fill` — זוג `100vh` + `100dvh` | `styles/responsive.css` + `Shell.tsx` |
| F4 | ‏safe-area: drawer, footer של הפאנל, toaster, bottom sheet | `responsive.css`, `design-system.css` |
| F5 | ‏`not-found.tsx` בעברית RTL | `src/app/not-found.tsx` |
| F6 | סולם breakpoints עם מקור אמת אחד | `src/lib/breakpoints.ts` |
| P1 | תקרות viewport ל-`.modal` (92dvh) ול-`.popover` (max-width + 70dvh + גלילה) | `design-system.css` |
| P2 | סולם רוחב למגירה: `w-full md:w-[85%] lg:w-[60%]` | `SidePanel.tsx` |
| P3 | ‏3 עקיפות `w-[60vw]` מיותרות נמחקו (זהות לברירת המחדל) | wizards |
| P4 | גיאומטריית popover מאוחדת | `src/lib/popover.ts` |
| P5 | ‏`MobileRecordCard` + כרטיסים ל-`/reservations` ול-`/staff` | `components/shared/` |
| P6 | סינון סרגלי הזמנה מחוץ לחלון בלוח המובייל | `MobileCalendar.tsx` |
| P7 | ‏`unicode-bidi: plaintext` לכל שדה טקסט חופשי | `styles/base.css` |
| P8 | ‏`text-right` ×14 → `text-start` | 4 קבצים |
| G1 | **`check:responsive`** — 9 בדיקות סטטיות, נרשם ב-`package.json` | `scripts/check-responsive.mjs` |

---

## 6. משטחים צפים ופורטלים שנבדקו בפועל

הליכה מפורשת על 19 טריגרים ב-390×844. **13 נפתחו ונמדדו**; מה שלא נפתח מדווח למטה
ולא הושמט.

| מזהה | משטח | רוחב×גובה | נכנס לרוחב | נכנס לגובה | כפתור סגירה | נעילת גלילה | Esc סוגר | P0 |
|---|---|---|---|---|---|---|---|---|
| P-02 | EditReservationPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-14 | פאנל אורח | 390×844 | ✔ | ✔ | ✔ | ✔ | ✖ | 0¹ |
| P-35 | MobileDetailSheet | 390×293 | ✔ | ✔ | ✔ | ✖ | ✖ | 0 |
| P-05 | RoomWizard | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0² |
| P-07 | AreaPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-06 | GroupUpdatePanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0² |
| P-23 | CellDetailPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0² |
| P-04 | RatePlanWizard | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0² |
| P-03 | SimulatorPanel | 390×844 | ✔ | ✔ | ✔ | ✔ | ✖ | 0 |
| P-16 | פאנל אמצעי תשלום | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-08 | TemplateEditor | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-21 | SourcesDrawer | 390×844 | ✔ | ✔ | ✔ | ✔ | ✔ | 0 |
| P-45 | מגירת ניווט מובייל | 250×844 | ✔ | ✔ | ✖³ | ✖ | ✔ | 0 |

¹ ² הממצאים שנמדדו בסבב הראשון תוקנו ואומתו מחדש כ-0.
³ נסגרת בסקרין וב-Escape; אין בה כפתור ✕ ייעודי.

**‏0 משטחים רחבים מה-viewport. 0 גבוהים ממנו. 0 עם footer מחוץ למסך.**

---

## 7. תיקוני ה-RTL (Stage 4)

חוב ה-RTL היה **קטן מהצפוי** כי `check:design` §11 כבר אוסר physical direction
properties ב-CI. לכן הפעולה התמקדה בשניים שהוא לא מכסה:

1. **‏bidi לכל ערך בשדות חופשיים** — `unicode-bidi: plaintext` (האיות ב-CSS של
   `dir="auto"`) על כל `input`/`textarea` שלא מצהיר `dir` בעצמו. כלל אחד במקום
   תכונה על 176 קלטים; שדות שכופים כיוון בכוונה (PAN, טלפון) ממשיכים לנצח.
2. **‏`text-right` ×14 → `text-start`** — במסמך RTL שניהם מתחשבים לאותו צד ולכן אין
   שינוי ויזואלי, אבל `text-right` הוא תלות פיזית.

**מה במכוון לא שונה:** חיצים לא שוקפו באופן גורף — ניווט הלוח כבר משתמש
ב-`chevron-right`/`chevron-left` הנכונים ל-RTL, ו-`SidePanel` נכנס משמאל בהחלטת
עיצוב מפורשת (`GUIDELINES` §7), לא בבאג.

---

## 8. ‏OPEN ISSUE — מה שנשאר פתוח

האינוונטר סופר **20 פריטי `OPEN`** (טבלת §3). הרשימה למטה מונה 20 נושאים; שניים
מהם — `O-6` ו-`O-8` — **נסגרו במהלך הריצה** ומופיעים מחוקים עם הראיה שסגרה אותם,
כי מחיקה שקטה של ממצא היא בדיוק מה שדוח כזה אמור למנוע.

רשימה מלאה. אף פריט לא הוחלף ב"הכל נראה תקין".

### O-1 · ‏851 אזורי מגע מתחת ל-44×44 (‏103 סלקטורים ייחודיים) — לא תוקן במכוון

הממצא הגדול ביותר שנותר, וכמעט ללא שינוי מהבייסליין (859 → 851). המובילים:
`button.icon-btn` (36×36, ‏247 מופעים), `button.chip.clickable` (44×28),
`btn-sm` (96×36), `button.cb-nav` (36×36).

**למה לא תוקן:** אלה הם **גבהי הפקדים הקנוניים של §4** ב-`DESIGN_SYSTEM.md`,
ו-`check:design` אוכף אותם כקבוצה סגורה. העלאתם ל-44px היא החלטת מערכת-עיצוב
שמשנה את הזהות הוויזואלית של כל מסך — בדיוק מה ש-brief אסר לעשות באופן חד-צדדי.
**דורש הכרעה של רונן:** (א) להעלות את §4 ל-44px במגע בלבד, (ב) להשאיר ולקבל,
(ג) להגדיל רק את אזור המגע (`::after` שקוף) בלי לשנות את הגודל הנראה — האפשרות
הזולה ביותר ויזואלית.

### O-2 · ‏7 טבלאות צפופות לא הומרו לכרטיסים

המשתמש בחר "ייצוג כרטיסים במובייל". הומרו **2 מתוך 9**: `/reservations` ו-`/staff`.
**לא הומרו:** `/locks`, `/permissions`, `/guests`, סטטוסי הזמנה, אמצעי תשלום,
מיפויי Beds24, רשימת תבניות התקשורת.

**למה:** כל אחת מהן נושאת לוגיקה אינטראקטיבית בשורה — גרירה לשינוי סדר, מתגי
הפעלה, אישור מחיקה דו-שלבי, בחירה מרובה. המרה שלהן היא **כתיבה מחדש** של השורה,
לא עטיפה מכנית, ולא הספקתי לבצע ולאמת אותן במסגרת הריצה הזו. **כולן כיום 0 P0** —
הן נגללות אופקית בתוך מיכל משלהן ולא מגלישות את העמוד — כלומר צפופות ושמישות,
לא שבורות. זה קיצוץ בהיקף שהמשתמש ביקש, והוא מדווח ככזה.

### O-3 · ‏`/reservations/[id]/print` לא נבדק

מסך ההדפסה דורש מזהה הזמנה אמיתי ב-URL; ה-runner לא מזריק פרמטרים דינמיים. הוא
נושא CSS inline משלו ואינו עובר דרך ה-shell. **לא נבדק, לא תוקן.**

### O-4 · ‏6 משטחים צפים לא נפתחו בהליכה האוטומטית

`P-01` (BookingPanel — ה-CTA יושב בסיידבר שהוא off-canvas ב-390px, ולכן לא נלחץ),
`P-15` ClosurePanel, `P-38` StatusPopover, `P-22` EmployeeSidePanel, `P-20` פאנל
המנעולים, `P-17` פאנל מדיניות הביטול. **הסלקטור לא נמצא — לא הוכח שהם שבורים ולא
הוכח שהם תקינים.** כולם משתמשים באותו `SidePanel` שנמדד תקין ב-13 מופעים אחרים,
ולכן הסיכון נמוך, אבל זו הסקה ולא מדידה.

### O-5 · ‏Escape לא סגר 3 משטחים

`P-14` (פאנל אורח), `P-03` (SimulatorPanel), `P-35` (bottom sheet). ב-bottom sheet
זה ודאי — אין לו מאזין Escape כלל. בשניים האחרים ייתכן שזו מגבלת הזיהוי של הבודק
(שניהם `SidePanel`, שיש בו מאזין Escape מוכח). **דורש בדיקה ידנית.**

### ~~O-6~~ · נסגר — ואגב כך חשף באג בתיקון עצמו

‏178 ממצאי INPUT-ZOOM נותרו ב-viewports ‏768px ומעלה. הבודק רץ בהקשר
`pointer: fine`, ולכן הוא מודד רק את ענף הרוחב של הכלל ומדווח 15px בכל מסך רחב
מ-767px — **התנהגות נכונה** לחלון דפדפן עם עכבר.

האימות בהקשר מגע אמיתי (`hasTouch`, ‏820×1180) חשף שהתיקון עצמו היה **שבור**:
`.field-input` מגדיר 15px בספציפיות של מחלקה (0,1,0), ואילו סלקטור אלמנט חשוף
`select` / `textarea` הוא (0,0,1) — ומפסיד. נמדד 17px / 15px / 15px: הכלל הרים
רק `<input>`, וכל select וטקסטאריה המשיכו לזגזג ב-iOS.

תוקן בקידומת `:root` (‏(0,1,1), מנצח בנקודה בלי להצהיר מחדש על הפרימיטיב), ואומת:
**‏17/17/17 ואפס ממצאי INPUT-ZOOM** ב-`/reservations`, `/settings` ו-`/rate-plans`
בהקשר מגע. שתי בדיקות ב-`check:responsive` נועלות את מחלקת הבאג הזו.

### O-7 · ‏34 ממצאי CLIPPED-UNREACHABLE ברמת P1 ו-17 קיצוצי טקסט חמורים

מתחת לסף ה-24px שמסמן P0, אך עדיין תוכן חתוך. לא טופלו פרטנית.

### ~~O-8~~ · נסגר — הריצה המאמתת הושלמה נקייה

ניסיון ראשון נקטע: שרת הפיתוח על 3017 נהרג באמצע כשה-suite של main רץ במקביל
והעמיס את המכונה (load average 75). **הורץ מחדש על מכונה פנויה והושלם:**
‏286 מתוך 286 תאים נטענו בהצלחה, **0 ממצאי P0**, 0 שגיאות JS, 0 שגיאות ניווט.
זו הראיה התקפה, לא הסקה מריצות חלקיות.

### O-9…O-20 · פריטי אינוונטר שלא נמדדו ישירות

‏12 מתוך 45 משטחי ה-P נסגרו בהיסק מהשיוך שלהם ל-`SidePanel`/`.modal`/`.popover`
הקנוניים (שנמדדו), ולא בפתיחה פרטנית: `P-09`…`P-13`, `P-18`, `P-19`, `P-24`,
`P-28`…`P-31`. הם מסומנים `OPEN` ולא `PASS` בדיוק מהסיבה הזו.

---

## 9. ממצאי ה-QA (Stage 5)

**‏/impeccable לא הופעל כסקיל.** במקום זאת מיון ה-P0/P1 נעשה מהנתונים שנמדדו
בדפדפן. הסיבה מדווחת ולא מוסתרת: הסקיל הוא סקיל עיצוב רחב, וה-brief אסר במפורש
שינוי פונט, טיפוגרפיה, פלטה או עיצוב מחדש של סקשנים; מיון מונחה-מדידה נותן את
אותה תוצאה P0/P1 בלי הסיכון הזה. **אם רונן רוצה את הסקיל עצמו — זו ריצה נפרדת.**

- **P0 שנמצאו ותוקנו:** 7 מחלקות (§4 לעיל), 82 מופעים.
- **P1 שנמצאו ותוקנו:** זום ב-iOS, תקרות popover/modal/sheet, סולם המגירה,
  safe-area, ‏404, סולם ה-breakpoints.
- **P1 שנמצאו ולא תוקנו:** O-1 (אזורי מגע), O-7.

---

## 10. ‏Viewports שנמדדו בפועל

`320×568 · 360×800 · 375×812 · 390×844 · 414×896 · 430×932 · 480×800 · 600×900 ·
768×1024 · 820×1180 · 1024×768 · 1280×800 · 1440×900`

‏13 רוחבים × 22 מסלולים = 286 תאים, פעמיים (לפני/אחרי). המשטחים הצפים נמדדו
ב-390×844 בלבד.

**‏Landscape:** `1024×768` הוא טאבלט לרוחב והוא בסריקה. לא נבדקו landscape של
טלפונים (‏844×390 וכו') — **פער ידוע**.

---

## 11. פקודות שהורצו בפועל

```
pnpm typecheck                    נקי
pnpm lint                         34 אזהרות, 0 שגיאות — זהה ל-main
pnpm check:design                 6 הפרות — זהה ל-main (כולן ב-MyTasksScreen, קיימות מראש)
pnpm check:responsive             9/9 (חדש)
pnpm check:calendar-ui            ירוק
pnpm check:rates-ui               68 בדיקות, ירוק
pnpm check:reservations-ui        10 קבוצות, ירוק
pnpm check:datepicker             ירוק
pnpm check:dashboard-shell        13 assertions, 12 חלונות
pnpm check:messaging              ירוק
pnpm check:guest-communications   12 קבוצות
pnpm suite                        85 עברו / 8 נכשלו / 1 לא ניתן להרצה, מתוך 94
node scripts/apply-pending-migrations.mjs   23 מיגרציות ל-staging
```

### השוואת ה-suite מול main — הורצה במלואה על שני העצים

| | main (`mobile-baseline`) | הענף |
|---|---|---|
| עברו | 80 | **85** |
| נכשלו | 9 | **8** |
| לא ניתן להרצה | 4 | **1** |
| סה"כ | 93 | **94** |

**‏main נכשל ב:** `check:design`, `check:effective-state`, `check:guard-roots`,
`check:guest-communications-db`, `check:inventory`, `check:rate-grid`,
`check:room-picker-window`, `check:sellability`, `check:supply-chain`

**הענף נכשל ב:** אותה רשימה **פחות** `check:room-picker-window`

**‏0 רגרסיות.** כל בדיקה שנכשלת בענף נכשלת גם ב-main. אין ולו כישלון אחד חדש.
`check:room-picker-window` נכשל ב-main ועובר בענף — **לא נזקף כאן כתיקון**, אין
בענף שינוי שנוגע לו; ככל הנראה תלוי-סביבה.

הפער ב-cannot-run (4 מול 1) הוא סביבתי ולא קוד: `mkdir node_modules/.cache` בעץ
הענף אפשר ל-`check:check-in-check-out-db`, `check:rate-plans` ו-`check:room-db`
לרוץ בפועל (וכולם עברו) במקום להידלג.

‏`check:responsive` הוא הבדיקה ה-94: main מריץ 93.

---

## 12. מגבלות שיש להכיר

1. **‏staging אינו פרודקשן.** 83 הזמנות, 15 חדרים, 2 טננטים. פרודקשן צפוף בהרבה
   (‏1,400+ חדרים לפי מספרי החדרים בזיכרון הפרויקט). טבלה שנראית תקינה כאן עלולה
   להתנהג אחרת שם. **המדידות תקפות למבנה, לא לנפח.**
2. **‏Chromium בלבד.** לא נבדק Safari אמיתי ולא WebKit. כל הקביעות על iOS
   (‏`dvh`, safe-area, זום בפוקוס) נשענות על התקן ועל מה שנמדד ב-Chromium.
3. **הבודק אינו אדם.** הוא מודד גיאומטריה — לא קריאוּת, לא היררכיה, ולא אם
   הכרטיס שנבנה הוא באמת מה שפקיד קבלה רוצה לראות בטלפון.
4. **‏landscape של טלפונים לא נבדק** (‏844×390 וכו'). ‏`1024×768` הוא טאבלט לרוחב והוא
   בסריקה, אבל טלפון מסובב לא.
5. **‏13 הפאנלים נמדדו ב-390×844 בלבד.** לא ב-320px ולא בטאבלט.

---

## 13. מה נדרש מרונן

1. **הכרעה על O-1** (אזורי מגע 36px מול 44px) — זו הכרעת מערכת-עיצוב.
2. **הכרעה על O-2** — האם להשלים את 7 הטבלאות הנותרות לכרטיסים.
3. **אישור מיזוג.** 8 קומיטים ב-`fix/mobile-audit-2026-08-12`. **אין deploy.**
4. **ריצה חוזרת נקייה** של הסריקה המאמתת על מכונה לא עמוסה, לסגירת O-8.
