# STATE — מצב עבודה נוכחי

מסמך מצב קצר: מה קפוא, מה בעדיפות, ומה אסור לגעת בו. עדכני נכון ל‑2026‑08‑27.

## עדיפות נוכחית

**אין עדיפות בלעדית מוצהרת.** המיקוד שנרשם כאן ב‑2026‑07‑18 — "הסמכת
Channex" — **בטל**: ‏Channex הוסר כליל מהמוצר (‏D91, מיגרציה 054 שינתה את
‏`channex_*` ל‑`external_*`), ו‑Beds24 הוא ספק הערוצים היחיד בפרודקשן.
העבודה מתנהלת פר‑משימה; מקור האמת לסדר היום הוא DECISIONS.md.

## מודולים מוקפאים

### מסך העובד `/housekeeping/my-tasks` — מוקפא (החלטת הבעלים)

> "מודול ניקיון/משימות מוקפא לפי החלטת הבעלים — ממתין לאיפיון UI מהבעלים;
> לא לגעת עד הודעה חדשה."

המשפט הזה הוא ההחלטה הקנונית, והוא מצוטט מכאן בשני מקומות:
‏`scripts/check-design-system.baseline` ו‑DECISIONS.md ‏D159. **אין לשנות את
נוסחו** בלי לעדכן את שניהם.

**מה עדיין קפוא בפועל:** `src/app/housekeeping/my-tasks/MyTasksScreen.tsx` —
מסך המשימות של העובד בנייד. הוא מחוץ לסיידבר מלכתחילה; הכניסה היחידה אליו
היא ניתוב אוטומטי של תפקיד `cleaner` בכניסה (`src/app/page.tsx`,
`src/app/(dashboard)/layout.tsx`). ‏6 הפרות `check:design` בקובץ הזה רשומות
כחוב מוצהר ב‑`scripts/check-design-system.baseline` (‏D159) — הן נולדו עם
הקובץ ב‑`b82eac2`, אינן רגרסיה, וכל הפרה **חדשה** בו עדיין מאדימה.

**מה כבר לא קפוא** (הרישום הקודם התיישן):

- **‏`/housekeeping` ו‑`/maintenance` גלויים בסיידבר** תחת מקטע "תפעול" —
  שני לוחות שיבוץ drag‑and‑drop על אותו `housekeeping_tasks` ‏(D88, ‏D88.1).
- **‏`/tasks` נמחק** — לא הוסתר. המסלול, פריט הסיידבר ומסלול `scope="all"`
  הוסרו ב‑D88.1; מודול משימות נפרד ייבנה במקום אחר.

### מקטע "ניהול עסקי" — לא נבנה

מסמכים · כספים · ספקים · דוחות — כל המקטע `hidden: true` ב‑
`src/components/layout/nav-items.ts`. אין מאחוריו מסלולים.

### מוסתרים בבקשת הבעלים (הקוד חי)

- **אורחים** (`/guests`) — מוסתר מהתפריט, המסך עצמו עובד.
- **חסימות חדרים** · **נוכחות** · **אישור בקשות** — פריטים בלי מסלול בנוי.

## סביבת ההרצה

`/var/www/guesthub` הוא **עץ הפרודקשן** (`.production-runtime`). ראה את פרק
‏"Production Runtime" ב‑CLAUDE.md ואת ‏D147: בנייה ופריסה רק דרך
`PROD_DEPLOY_OK=1 npm run deploy:prod`, ואין worktree בלי בקשה מפורשת.
