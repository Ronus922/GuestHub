# TESTS — Booking Window V2 (סוכן TESTER)

תאריך: 2026-07-26 · ענף `feat/booking-window-v2`.
**הראיה הקבועה היא שומר שרץ** — כל שורת מטריצה ממופה לשומר ולאסרשן שמריץ אותה
על עותק :5433 (לעולם לא פרודקשן). מספרים מפורשים בכל תרחיש.

## 1 · מטריצת חישוב — מצבי מחיר × הנחות × מע״מ

בסיס קבוע: שני לילות 500+520 = ₪1,020 לינה (הפיקסצ׳ר של השומרים).

| תרחיש | מספרים | שומר · אסרשן |
|---|---|---|
| auto (מנוע) | 1020 → total 1020 | `check:pricing-engine` #8 |
| manual_night | ‏350×2 = 700; snapshot מתעד mode+מבצע | `check:pricing-equality` #22 |
| manual_total מדויק לאגורה | ‏1000.01 נשמר בדיוק; rate=500.01 תצוגה בלבד; LOS פטור | `check:pricing-equality` #23 |
| הנחה none ("מחיר מלא") | value מוזן נזרק, total 1020 | `check:pricing-equality` #18b |
| amount_total ₪120 | ‏1020−120 = 900 | #18b |
| amount_per_night ₪40 | ‏40×3 לילות = ₪120 הנחה | #18b (סכימת לילות מרובת-חדרים) |
| percent_total 10% | ‏1020−102 = 918 | #18b |
| percent_per_night 10% | ‏102 — שקילות מתועדת ל-percent_total | #18b |
| מע״מ כולל 18% אחרי הנחה | ‏VAT(900) = ₪137, נטו 763 | #18b |
| פטור ממע״מ | אותו total ‏900; VAT=0; נטו=900 (הטוגל לא מזיז מחיר) | #18b |
| הנחה > סכום | נדחה בשרת ("ההנחה גדולה מסכום ההזמנה") | #18b throws |
| אחוז 101 / ערך שלילי | נדחים בשרת | #18b throws |
| סכום OTA ריבוני | override 1234.56 + הנחה 4000 → floor 0, בלי דחייה | #18b (validate:false) |
| יתרה חלקי/עודף | ‏918−300=618 · ‏918−1100=−80 (זיכוי, לא מרוצף) ≡ ledger | #18b + `check:totals-parity` #4 |
| מטבע לא-ILS + exchange_rate | נכתבים מהקלט; ולידציית enabled + חובת-ידני בשרת (D107) | pins ב-`check:pricing-equality` + סכימת 058 CHECKs |

## 2 · LOS אוטומטי

| תרחיש | מספרים | שומר |
|---|---|---|
| מדרגה נבחרת מהלילות | ‏2 לילות ≥ מדרגת 2+, ‏10% מ-1020 = ‏−102 → 918 | `check:pricing-engine` #34 |
| קצר מהסף → כלום | לילה אחד → אין הנחה, 500 | #34 |
| שתי מדרגות חופפות → הגבוהה | ‏3+ (25%) גובר על ‏2+ (10%) | #34 |
| מדרגת תוכנית מחליפה דיפולט | plan-tier ‏₪50×2 = ‏−100 | #34 |
| בסיס = לינה בלבד | ‏10% מ-1020, לא מ-1220 (אורח נוסף בחוץ) | #34 |
| override ידני פטור | ‏manualRate 300 → אין LOS | #34 |
| cap לסכום הלינה | ‏99,999 לשהות → ‏−1020, total 0 | #34 |
| **לא נערם על derived_percentage** | ‏NR ‏−10%: דיפולט לא חל (918); tier מפורש על התוכנית כן | #34 (D105) |
| ‏seed 7→15% · 30→30% | idempotent, ‏ON CONFLICT partial-unique | מיגרציה 057; הוחל על העותק |
| ‏LOS נחתם ב-snapshot | ‏losDiscount נשמר, עריכת מדרגה לא משכתבת הזמנה | `check:pricing-equality` #3b |

## 3 · רגרסיה — פרודקשן לא זז

| ראיה | תוצאה |
|---|---|
| `check:totals-parity` על עותק pg_dump אמיתי | ‏**42/42 הזמנות בייט-זהות** בעמודות הכסף על פני 057+058; replay מדויק; ledger ללא תזוזה. פלט committed: `parity-run-2026-07-26.txt` |
| 059 על העותק | 2 הערות חיוב הועברו + רדקציית CVV; ‏0 תבניות CVV בהערות |
| `check:pricing-engine` | 36/36 |
| `check:pricing-equality` | 25/25 (כולל pins שאין נוסחת סה״כ מחוץ למקור) |
| `check:room-picker-window`, `check:rates-ui` | ירוקים |
| `check:cards` | נופל רק בנקודת ה-PSP הבסיסית (בסיס #60 — קדם לענף) |

## 4 · אתר ציבורי

`check:public-quote` (חדש, 3 בדיקות על מודולים מקומפלים אמיתיים):
ציטוט = מנוע כולל כסף שה-ESS לא רואה (אורח נוסף: 1220 ≠ 1020) ·
ציטוט ≡ חיוב (priceReservationStays מחייב את אותו מספר) ·
מדרגת LOS זהה בשני המשטחים (אותו tier id, אותו סכום).

## 5 · ערוץ וכרטיסים

| תרחיש | שומר |
|---|---|
| ‏OTA total ריבוני + הנחות מקומיות נשמרות במודיפיקציה | pins `check:pricing-equality` + מסלול booking-import דרך המקור היחיד |
| קליטת כרטיס ערוץ מוצפנת, בלי CVV, audit ממוסך | `check:channel-card-ingest` |
| ‏re-ingest מנקה CVV ידני ישן | `check:channel-card-ingest` (תרחיש V2) |
| החלפה ידנית מנקה חלון חיוב/ref | pin `check:card-save-flow` |
| ‏D108 + כשל גלוי + D109 + סדר כרטיסים | `check:card-save-flow` 6/6 |

## 6 · E2E — מה הוכח איך

- **התנהגות מלאה (יצירה/עריכה/הזזה/ייבוא ערוץ/שמירת כרטיס/ציטוט ציבורי)** —
  מוכחת ברמת ה-server actions/מודולים האמיתיים על עותק הפרודקשן (השומרים לעיל
  מריצים את אותם קבצים שהאפליקציה מריצה, כולל schema אמיתי ו-rollback).
- **רנדור אמיתי של הבקרות** — `scripts/render-pricing-evidence.mjs` מרנדר את
  `PricingControls` האמיתי (React + Tailwind האמיתי + bw-*) ב-Chrome headless,
  מאמת RTL / 3 מצבים / 4 יחידות / טוגל / 3 קופסאות יתרה / ltr-num, ומצלם:
  `shots/impl-pricing-controls-{desktop,mobile}.png` (committed).
- **קליק-תרו מאומת מלא בדפדפן** דורש session של Supabase (אין bypass ואין
  משתמש בדיקה; יצירת משתמש ב-auth המשותף היא כתיבה לפרודקשן שלא בוצעה בכוונה).
  זהו הפריט היחיד שנשאר לאימות ידני של רונן אחרי המיזוג — צ׳קליסט של
  שתי דקות בסוף SIGNOFF.md.
